# Анимации: план (09.08.2026)

> Повод: владелец собрал 9 компонентов с ui.watermelon.sh и спросил, стоит ли
> добавлять такое в Recall. Файл-источник: `D:\Downloads\sounds\фыва.md`.
>
> Задача, как её поставил владелец: разобрать, какие анимации тянут ненужные
> зависимости; что из этого я могу написать сам; нужна ли нам каждая вообще.
> Делать только то, что не ломает работающее и не замедляет загрузку.

---

## 1. Мерка, по которой решаем

Анимация в Recall проходит, если выполнены **все четыре** условия:

1. **Отвечает на вопрос пользователя.** «Что происходит?», «куда я попал?»,
   «моё действие сработало?», «это надолго?». Анимация, которая не отвечает ни
   на один из них, — украшение, и её нет.
2. **Ноль новых зависимостей** либо доказанная невозможность обойтись. У нас
   сегодня ноль UI-библиотек: иконки — свои 215 строк инлайн-SVG
   (`components/icons.tsx`), анимации — 14 keyframes в `index.css`.
3. **Не растит стартовый бандл.** Замер до работы: `index-*.js` **337 929 Б,
   gzip 105 241**. Это то, что качает человек при первом заходе с телефона.
4. **Уважает `prefers-reduced-motion`.** Блок уже есть в `index.css:223` —
   любая новинка обязана в него попадать, иначе мы своими руками ломаем
   доступность, за которую два аудита боролись до нуля замечаний.

Отдельное правило, выученное на этом проекте: **анимация не должна двигать
раскладку**. Всё на `transform` и `opacity` — они считаются композитором и не
вызывают пересчёт страницы. `height`, `top`, `width` в анимациях не используем.

---

## 2. Разбор девяти компонентов владельца

Все девять написаны на `motion/react` (Framer Motion). Сверх неё в импортах:
`lucide-react`, `react-icons` (пять разных наборов: `ri`, `rx`, `fa`, `fa6`,
`tb`, `bs`, `io5`, `md`), `@hugeicons/react` + `@hugeicons/core-free-icons`,
`react-use-measure`, `cn` из shadcn-набора.

| # | Компонент | Что тянет | Напишу сам? | Нужен нам? |
|---|---|---|---|---|
| 1 | ActivitiesCard | motion, lucide | да | **нет** — списки у нас на `RowCard`, второй визуальный язык мы сами же ругали в ревью студии |
| 2 | MinimalCarousel | motion, lucide | частично (shared-element даёт View Transitions) | **нет** — сетки «разверни одну карточку» в продукте нет |
| 3 | ProfileCard | motion, lucide, react-icons ×2 | да | **нет** — карточка CRM, под Nocturne переписывается целиком |
| 4 | **CopyConfirm** | framer-motion, lucide | **да, тривиально** | **да** — код приглашения у преподавателя |
| 5 | DisclosureCard | motion, lucide, react-icons ×5, react-use-measure | да (`grid-template-rows: 0fr→1fr` заменяет измерение) | **частично** — приём пригодится раскрывашкам студии |
| 6 | FeedbackComponent | motion, lucide, hugeicons ×2, react-icons | да | **отдельный разговор** — брать стоит не ради анимации, а потому что способа собрать обратную связь у нас нет вообще |
| 7 | VoiceNote | motion, lucide, react-icons ×2 | да | **нет** — «Речь» на Web Speech API, там нет ни blob, ни плеера |
| 8 | AdaptiveSlider | motion, `cn` | да | **нет** — скорость озвучки и размер текста у нас чипами, на тач-экране чипы точнее |
| 8б | **AnimatedNumber** (внутри того же файла) | motion | **да** | **да** — энергия ⚡ и стрик сейчас меняются скачком |
| 9 | **ContinuousTabs** | motion (`LayoutGroup`) | **да** | **да** — см. ниже, у нас это БЫЛО и потерялось |

**Итог: из девяти берём три, и все три пишутся без единой зависимости.**

Ключевой приём во всех девяти — `layoutId` Framer Motion: элемент «перетекает»
из одного места в другое. Ровно это умеет **View Transitions API** через
`view-transition-name`, встроенный в браузер, ценой ноль килобайт. Так что
библиотека нужна не ради возможностей, а ради удобства записи.

### Находка при разборе

`BottomNav.tsx:89` — активная вкладка сейчас просто меняет цвет фона.
**Скользящего индикатора нет**, хотя `CLAUDE.md` уверяет, что «плавающий
градиентный кружок-индикатор скользит к активной вкладке, иконка
приподнимается». Он был и пропал при переходе на Nocturne / четыре вкладки.
То есть `ContinuousTabs` владельца — это то, что у нас уже было.

Это второй за неделю случай, когда документ отстал от кода (первый — журнал
`docs/findings.md`). Поэтому обновление `CLAUDE.md` идёт до работы, а не после.

---

## 3. Чего в списке владельца не было, а сделать нужнее

Отсортировано по тому, сколько людей это почувствует.

### 3.1. Переходы между экранами — главное

Сейчас любой тап по строке — мгновенная подмена: список исчез, экран появился.
Это главное, чем веб-страница отличается от приложения.

**Точка вставки одна.** Внутренние экраны продукта ходят не через `<Link>`, а
через `lib/useUrlState.ts` — его используют **12 экранов**: «Учёба», читалки
EN/ES, «Практика», грамматика (уроки, спряжения, неправильные и фразовые
глаголы), студия, материалы, задания, квесты. Значит `document.startViewTransition`
достаточно обернуть вокруг одного `setParams`, и плавными становятся все
переходы разом — без правки двенадцати файлов.

Отдельно `<Link>` (27 штук в 12 файлах) и `useNavigate` (25 вызовов) — это
переходы между вкладками; их трогаем во вторую очередь и по решению, нужны ли
там переходы вообще (между вкладками плавность может, наоборот, мешать).

Поддержка: Chrome/Edge и Safari 18+. Где API нет — переход просто не играет,
содержимое меняется как раньше. Ломаться нечему.

⚠️ React Router у нас `<BrowserRouter>` (не data-router), поэтому встроенный
проп `viewTransition` у `<Link>` не сработает — он живёт в data-router. Пишем
свою обёртку с `flushSync`; это ~20 строк и не требует переезда роутинга.

### 3.2. Ожидание ответа AI

`ConversationPage.tsx:318` — «печатает…» статическим текстом. Человек ждёт
ответа 3–8 секунд и смотрит в это место дольше, чем в любую карточку.
Три прыгающие точки отвечают на вопрос «оно вообще живое?».

Те же места: квесты, разбор письменной работы, генерация материала у
преподавателя (там ожидание доходит до полуминуты).

### 3.3. Момент правильного ответа

В играх и упражнениях верно/неверно — смена цвета. Момент правильного ответа —
единственное место, где у обучающего приложения есть право на маленький
праздник, и оно не использовано: конфетти есть только на финале раунда.

### 3.4. Живые числа

`EnergyBar.tsx` — `{left} из {cap}` меняется скачком, полоска тоже. Энергия
это валюта продукта; валюта должна ощущаться. Сюда же стрик на Главной.

---

## 4. Что решено НЕ делать

- **Не подключать Framer Motion.** Ориентировочно +30–40 КБ gzip к стартовым
  105 — треть сверху, ради трёх компонентов, которые пишутся руками. Ленивая
  загрузка сняла бы вес со старта, но оставила бы зависимость, которую дальше
  тянуть везде.
- **Не переносить карточки-дашборды** (1, 2, 3, 5, 7). Они из другого продукта
  и другого визуального языка; под Nocturne от исходника остаётся один
  `layoutId`.
- **Не заводить иконочные библиотеки.** Нужные знаки дорисовываются в
  `components/icons.tsx` — там уже весь набор в одном стиле.
- **Не анимировать печать.** `PrintSheet` остаётся белым и статичным.

---

## 5. Порядок работы

1. Этот план.
2. **`CLAUDE.md`** — привести к тому, что есть в коде (он отстал на ~20
   коммитов, две неточности уже пойманы). Делается ДО работы: я читаю его в
   начале каждой сессии, и ошибки в нём стоят дороже, чем в любом другом файле.
3. Ресёрч по коду: пройти экраны и назвать конкретные места — где анимация
   отвечает на вопрос пользователя, а где навредит.
4. Реализация с замером бандла до и после.
5. Смоуки + `scripts/ux-audit.mjs`; отдельно проверить, что при включённом
   «уменьшить движение» всё выключается.
