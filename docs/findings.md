# Журнал находок — баги, оптимизации, улучшения

> Сюда падают находки ВСЕХ аудитов (заходы учебника, ревью, пентесты),
> которые ждут решения владельца. Прочитал → решил → вычеркнул (или перенёс
> в план заходов). Формат: 🔴 важно/опасно · 🟡 стоит сделать · ⚪ по желанию.
> Исправленное переносить в раздел «Закрыто» внизу (с датой).

## Открыто

### UX-ревью, заход 1А: вход и первые 15 минут (2026-08-06)

Полный разбор — `docs/mkt/20-ux-review-1-newbie.md`. Прогон живой, с замерами.

- 🔴 **«Начать первое занятие» не начинает занятие.** Кнопка ведёт на хаб
  «Практика» (витрина из 12 плиток), и только через ~5 секунд экран САМ
  меняется на список текстов. Логика пропуска пустого шага есть, но решение
  принимается ПОСЛЕ перехода: два последовательных запроса, и всё это время
  человек смотрит в меню. Ткнёт в плитку — экран сменится под пальцем.
  Фикс: решать в онбординге (у новичка слов нет по определению) и стартовать
  сессию сразу с чтения.
- 🟡 **Шаг «уровень»: тупик заметнее выхода.** Кнопка «Дальше» отключена и
  серая без объяснения, «Пропустить» — мелкая ссылка под ней. Либо подпись
  «выбери уровень или пропусти», либо сделать пропуск полноценной кнопкой.
- 🟡 **Первое, что предлагают новичку, — уйти с экрана.** На хабе «Практика»:
  «Игры используют твои слова… Добавить слова — в Учёба → Слова». Нужна кнопка
  действия прямо здесь («Взять 10 слов моего уровня»), а не инструкция.
  Заодно скрывать у новичка заведомо пустые плитки («Мои ошибки — пока пусто»).
- 🟡 **Цель обучения не спрашивается.** Есть язык и уровень, нет «зачем»
  (IELTS / школа / работа / для себя). От этого зависят подбор текстов, промпт
  диалога и программа; преподаватель хотел бы видеть это в карточке первым.
  Идея владельца, подтверждена ревью.
- ⚪ **Чипы «Как ты о нас узнал» разрывают связку** «вот твой план → начать»:
  стоят между обещанием и кнопкой. Перенести после кнопки или на экран после
  первой сессии.
- ⚪ **В онбординге нет «назад»** — выбрал язык случайно, меняй потом в настройках.

### UX-ревью, заход 1Б: хабы «Учёба» и «Практика» (2026-08-06)

Полный разбор — `docs/mkt/20-ux-review-1b-hubs.md`. Прогон живой, три состояния
аккаунта: пустой, с 15 словами, и со всем назначенным от преподавателя.

- 🟡 **Кнопка врёт о том, что сделает.** В пустой колоде «Добавить первые
  слова» ведёт на корень хаба «Учёба», а не на экран добавления: до обещанного
  ещё два тапа (`src/features/flashcards/DeckReview.tsx:272`).
- 🟡 **«Слова» означает разное в соседних вкладках.** В «Учёбе» это управление
  словами (паки, свои слова), в «Практике» — тренировки. Повторение колоды
  доступно из обоих мест разным числом тапов (3 против 2), и ничто не
  подсказывает новичку, куда идти.
- 🟡 **На «Практике» ни одна из 13 плиток не видна без прокрутки** — первый
  экран целиком съеден заголовком, плиткой «Повторение» и абзацем-объяснением.
- 🟡 **«Пропущенное слово» и «Впиши слово» звучат почти одинаково**, а механика
  разная: в первом выбор из вариантов, во втором набор руками. Из названий это
  не следует.
- ⚪ «Значения» и «Быстрый перевод» тоже не сообщают, чем отличаются.
  «Мои ошибки» стоит среди словарных игр, но собирает только грамматику.
  У «Грамматики» единственной нет каретки возврата.

**Две ложные тревоги, снятые замером** (важно для будущих ревью): плавающая
навигация НЕ перекрывает плитки — это артефакт полностраничных скриншотов, при
реальной прокрутке минимальный зазор 3px; и «Значения» не зависают — просто
грузятся ~2 секунды через запасной путь, первый скриншот был снят слишком рано.

### UX-ревью, заход 1В: студия преподавателя (2026-08-06)

Полный разбор — `docs/mkt/20-ux-review-1v-studio.md`. Прогон живой, с замерами.

- 🔴 **Четвёртая вкладка «Методичка» обрезана на экране 390px.** Ряду вкладок
  нужно 415px, а места 348px; горизонтальной прокрутки у контейнера нет
  (`overflow-x: visible`), поэтому добраться до вкладки можно только сдвигая
  ВСЮ страницу вбок — без единой подсказки, что там что-то есть. То есть на
  телефоне методичка практически недоступна. Фикс: прокручиваемый ряд вкладок
  или перенос в меню.
- 🟡 **Название вкладки «Письмо» непонятно до нажатия** — читается как
  «сообщение ученику», а это задания-эссе с проверкой по критериям IELTS.
  Учитывая, что это наш сильнейший аргумент, название прячет ценность.
- 🟡 **Карточка ученика — 7 раскрывашек, в развёрнутом виде 4333px** против
  933px экрана: почти пять прокруток. Даже свёрнутая карточка требует прокрутки,
  чтобы увидеть второго ученика.
- 🟡 **Тест уровня стоит третьим**, хотя комментарии в самом коде и методичка
  говорят, что с нового ученика надо начинать именно с него.
- ⚪ **Новый ученик показан как «Уровень B1»** — это дефолт колонки
  `profiles.level`, а не результат теста. Строкой ниже написано «Тестов пока не
  было»: экран противоречит сам себе и вводит преподавателя в заблуждение.
- ⚪ Разный вид у однотипных разделов: «Тест уровня» — рамка, остальные шесть —
  текстовые ссылки со стрелкой.

### UX-ревью, заход 1Г: сквозная навигация (2026-08-07)

Полный разбор — `docs/mkt/20-ux-review-1g-navigation.md`. Вывод одной строкой:
**вперёд приложение ведёт нормально, назад — плохо.**

- 🔴 **Больше половины внутренних экранов не существует для истории браузера.**
  «Назад» из открытого текста → Главная (мимо списка и хаба), из упражнений
  задания → Главная с обнулёнными ответами, из урока грамматики → `/study`, из
  «Методички» преподавателя → Главная. В установленном PWA свайп-назад —
  единственный способ вернуться, и он прыгает не на шаг, а сразу в начало.
  Правильно работает только «Практика», где режим лежит в адресе (`?m=`), —
  этот же приём надо распространить.
- 🔴 **На стадии упражнений задания замерено 0 заголовков и 0 кнопок возврата** —
  не видно даже, какое задание выполняешь.
- 🟡 Каретка «назад» пушит новую запись в историю: вышел из «Спринта» кареткой,
  свайпнул назад — снова «Спринт».
- 🟡 Каретки жёстко ведут в «Учёбу», хотя самый частый вход — карточка на
  Главной. В коде УЖЕ есть готовый `useSmartBack`, который это решает.
- 🟡 **Офлайн «Учёба»:** 15 секунд пустых скелетонов, потом строки заданий,
  квестов и программы просто исчезают без объяснения (`catch(() => null)`), а
  уровень B1 превращается в «Определи свой уровень». Компонент `LoadError` в
  проекте есть, но хабу не достался.
- 🟡 F5 теряет начатое (раунд, ответы, открытый текст). У текста нет своей
  ссылки — преподаватель не может прислать «прочитай вот это».
- 🟡 Ссылка, открытая без входа, после логина забывается — попадаешь на Главную.
- ⚪ Адрес врёт после возврата кареткой (`?view=reader` остаётся); один экран
  зовётся тремя именами («Мои ученики» / «Преподаватель» / «Ученики»); 404 молча
  редиректит; `ErrorBoundary` не сбрасывается при смене маршрута.

**Хорошее:** 20 глубоких ссылок из 20 открылись осмысленным экраном без единой
JS-ошибки; повторный тап по активной вкладке возвращает в хаб (лучший
компенсатор находки 1); на настоящей сборке офлайн работают грамматика, игры и
каркас, а тексты про потерю сети написаны по-человечески.

### Ревью 2А: корректность механик и счётчиков (2026-08-07)

Полный разбор — `docs/mkt/20-review-2a-mechanics.md`. Метод — замер на живых
аккаунтах, настоящем ts-fsrs и настоящих RPC, а не чтение кода.

- 🔴 ~~Тексты про энергию разошлись с кодом~~ — **исправлено в тот же заход**
  (см. «Закрыто»). Оферта и «Тарифы» обещали плоские «30 ⚡ на весь триал» и
  «переводы без лимита всегда», хотя коммит `a07a6ad` того же дня ввёл ступеньку
  30→15 и суточные капы.
- 🟡 **`submit_word_check` не пересчитывает результат на сервере** — в отличие от
  `submit_material`. Ученик может подделать вызов RPC и показать учителю ложное
  «всё верно», заодно уйдя от штрафа FSRS. Это ровно тот класс дыры, который мы
  закрывали в ревью безопасности 20.07, — одно место пропустили.
- 🟡 **MCQ с дублирующимися по нормализации вариантами:** клиент считает по
  индексу и говорит «неверно», сервер считает по тексту и засчитывает «верно».
  Разошлось 1 сценарий из 8 в живом E2E.
- 🟡 **«Слов изучено» на /progress считает по-своему** — только `state==='review'`
  без порога 21 день, тогда как остальное приложение использует `statusOf`.
  Замерено: карточка попадает в `review` уже с интервалом 2 дня.
- ⚪ Два счётчика «к повторению» (Главная и /progress) по-разному считают новые
  карточки — на одном наборе 5 против 2.
- ⚪ `answerMatches` не сворачивает дефис, апостроф и финальную точку. Сейчас не
  бьёт: 0 из 385 fill-упражнений. Латентно — для будущего AI-контента.

**Проверено и работает верно:** ядро FSRS (интервалы, lapses, состояния),
возврат «Ещё раз» в тот же раунд, отсутствие поощрения расписания за верный
ответ в играх (проверены все 5 мест), эскалация промахов, баллы `submit_material`
(7 из 8 сценариев), анти-повтор слов, стрик с местным днём и серверным клэмпом,
лимиты энергии по всем тарифам (27/27 и 12/12 смоуков).

### Ревью 2В: тексты и сообщения об ошибках (2026-08-07)

Полный разбор — `docs/mkt/20-review-2v-texts.md` (6 🔴, 11 🟡, 5 ⚪; пять находок
подтверждены живыми пробами). Часть исправлена в тот же заход — см. «Закрыто».

- 🔴 ~~Экран входа отвечал по-английски~~ — **исправлено.** «Invalid login
  credentials» на неверный пароль было первым, что видел новый человек.
- 🔴 ~~«Минимум 6 символов» под полем пароля, база требует 8~~ — **исправлено.**
- 🔴 ~~Тарифные карточки обещали «переводы и произношение без лимитов»~~ —
  **исправлено.** Утренняя правка легла в подпись и оферту, а пункты самих
  карточек пропустила.
- 🔴 ~~Пункт A4: «До 25 AI-материалов в месяц», на деле ~12~~ — **исправлено в
  текстах.** Материал стоит ДВУХ генераций (план + текст), а задания «Письма»
  тихо тратят тот же бюджет (`src/lib/writing.ts:238,267`). Осталось открытым:
  счётчика генераций в интерфейсе учителя по-прежнему нет.
- 🔴 **Репетитор на первой генерации читает «Лимит генераций исчерпан».** У
  триал-учителя без учеников `gen_limit: 0` — механика верная (генерации
  включаются с первым учеником), врёт только текст, и нигде нет предупреждения.
- 🔴 **«TypeError: Failed to fetch» вместо человеческой ошибки** — 26 мест
  `throw new Error(error.message)` в `src/lib`. Воспроизведено: ученик,
  отправляющий работу при моргнувшей связи, увидит именно это. Предложена одна
  общая `dbError()`.
- 🟡 **База до сих пор говорит «ученица» и «вы»** — три RPC вживую ответили
  «Это не ваша ученица.» Переименование (`0b2c766`, 202 замены) прошло по
  `src/`, но `docs/schema.sql` не тронуло.
- 🟡 Платящему репетитору при сбое AI сообщают «это бывает в часы пик **на
  бесплатном тарифе**», показывают «(400)» и слово «Gemini»; ошибка
  конфигурации отдаёт наружу имя переменной `GROQ_API_KEY`.

### Ревью 2Б: качество AI-ответов (2026-08-07)

Полный разбор — `docs/mkt/20-review-2b-ai-quality.md`. 12 тяжёлых вызовов и 13
лёгких, генерация материалов и программ не запускалась (дорогая Pro-квота).
**🔴 нет ни одной.** Педагогически пустых ответов не выдал ни один прогон.

- 🟡 **Диалог иногда сдвигает время в исправлении:** «he don't like → he
  **didn't** like» там, где было настоящее (1 прогон из 3 на A1). Ошибка в
  исправлении опаснее пропущенной — новичок её не перепроверит.
- 🟡 **Выдумал несуществующую ошибку:** «too → [удалить]» в корректной фразе
  «My sister works in a school too».
- 🟡 **«Письмо» молча исправило 2 ошибки из 11** в «улучшенной версии», не
  назвав их в списке, — как раз согласование времён, которое надо объяснять.
- 🟡 **`profiles.level` по умолчанию `'B1'`** (`schema.sql:17`): новичок до
  placement общается с AI как B1. Для аудитории A1–A2 это первый экран мимо.
- 🟡 Определение для A1 может не пройти собственный фильтр `isUsable` (промпт
  просит ≤8 слов, фильтр требует ≥3 слов и ≥12 символов) — слово молча выпадает
  из игры «Значения».
- ⚪ Мелочи: «книгу» вместо «книга» на карточке, AI-разбор не прощает опечатки
  вопреки обещанию в CLAUDE.md, `parseReply` квеста не переживёт markdown
  вокруг `VERDICT`, один раз сорвался на «вы».

**Закрыта давняя заметка из CLAUDE.md** «испанский промпт иногда пропускает
ошибки»: не воспроизводится — Диалог ES поймал 6 из 6, EN 6 из 6 в каждом из
четырёх прогонов, «Письмо» 9 из 11. Подстройка под уровень работает вживую:
A1 → «My favorite book is Harry Potter», C1 → «The Shadow of the Wind by Carlos
Ruiz Zafón, an evocative mystery…»; `chair` для A1 — «you sit on it». Жалоба
про энциклопедичный «стул» снята. Контекстный перевод дал разное значение под
контекст на 10 многозначных словах из 10.

### Найдено попутно (2026-08-06)

- 🟡 **Режим преподавателя нельзя выключить.** Нажал из любопытства — остался
  со вкладкой навсегда. Нужна кнопка выключения, доступная, пока нет
  привязанных учеников (иначе можно осиротить студию).
- 🟡 **Обещание «до 25 материалов в месяц» неверно** (блокер A4 из
  `docs/mkt/19-fix-plan.md`): один материал = 2 генерации, реально 10–12.
  Чинить до первой продажи.
- ⚪ **Лендинг `/teachers` не продаёт проверку эссе и IELTS** — сильнейший
  аргумент отсутствует на странице, которая ведёт платящий сегмент.

### Осознанные компромиссы — починить ДО открытия для чужих учениц

- 🔴 **review_states пишется клиентом целиком** (lib/fsrs.ts). Браузер сам
  считает расписание FSRS (stability/difficulty/due/reps/lapses/state) и
  апсертит в базу; RLS проверяет лишь владение. Ученица через DevTools может
  поставить всей колоде state='review', lapses=0 — «выучить» всё мгновенно и
  подделать диагностику (lapses≥2) и отчёт родителям. **Решение владельца
  (27.07): отложить.** Обоснование: в закрытом тесте оба пользователя
  доверенные, а подделка вредит в первую очередь самой ученице. НО перед
  подключением ЧУЖИХ учениц это обязательно к починке — иначе отчёт, за который
  платит родитель, можно нарисовать. Фикс: серверный эндпоинт /api/review
  (тот же движок ts-fsrs считает на сервере, клиент шлёт только оценку
  again/good) — по образцу того, как submit_material считает балл на сервере.
  Родственно осознанному компромиссу «ответы упражнений уходят на клиент».
- 🔴 **word_checks — результат перепроверки считает клиент** (тот же класс, см.
  «Соседние места» ниже; подтверждено кодом). ХУЖЕ review_states: результат
  читает ПРЕПОДАВАТЕЛЬ. Чинить вместе с review_states ДО открытия для чужих —
  пересчётом ok на сервере в submit_word_check (у RPC есть card_ids + cards).

### Дыры покрытия тестами (аудит У5, 2026-07-27)

Общее правило нашлось: покрыто дешёвое+опасное (RLS/квоты/чистая логика),
не покрыто ТРУДНОЕ (ИИ-ответы, речь, деньги). По убыванию риска:
- 🟡 **answerMatches (lib/text.ts) без юнит-теста** — единственная точка сверки
  КАЖДОГО ответа (грамматика/глаголы/задания/баллы). Баг тихо исказит баллы
  разом по всему продукту. Дёшево закрыть юнитом (варианты через «/», order,
  диакритика) — стоит сделать в первую очередь.
- 🟡 **Биллинг-UI и админка (/pricing, /admin, admin_set_plan)** — реальные
  деньги, НИ ОДНОГО теста (backend-квоты покрыты, UI/выдача плана — нет).
  Ошибка = потерянные деньги или бесплатный premium всем. Смоук на admin_set_plan.
- ⚪ **Генерация материалов/программы от ИИ (materials.ts, studyPlan.ts)** — только
  «живой прогон руками» (жжёт квоту Pro). Автотест смысла невозможен, но можно
  тест САНИТАЙЗЕРА (мусорный/переполненный JSON от ИИ не роняет и вычищается).
- ⚪ **Речь/транскрипция (speech.ts, transcribe.ts)** — фича без единого теста.
- ⚪ Прочее слабое: контекстный словарь, определения, шаги онбординга, читалки
  обычных текстов, мультивыбор в читалках (покрыт только в «Моих текстах»).

Характер тестов (для памяти): НЕ ловят смысл ИИ-ответа, визуальные регрессии
кроме контраста/тач-целей, iOS/Safari-специфику, деградацию производительности,
и нет CI-гейта (обязателен только `npm run build`).

### Дыры эксплуатации (аудит У6, 2026-07-27) — к закрытию ДО платящих

Деплой и PWA-обновления сделаны зрело (автодеплой, аддитивные миграции,
reg.update, ErrorBoundary с авто-reload на ChunkLoadError). Но наблюдаемость и
восстановление — дыры, и вместе они самые опасные для продукта, берущего деньги:
- 🔴 **Нет сбора ошибок с прода** (ноль Sentry/аналогов; `console.error` во всём
  src ОДИН раз). Владелец узнаёт о поломке только от пользователя (с клиентами —
  от заплатившего). Vercel Analytics — это посещения, НЕ ошибки. Фикс дёшев:
  Sentry free-tier (узнавать о сбое от системы раньше клиента).
- 🔴 **Бэкапов БД фактически нет.** Supabase free — без ежедневных бэкапов;
  PITR — платное поверх Pro. Своего экспорта нет, восстановление не репетировали.
  Связка «молчаливый сбой + невосстановимая потеря данных» = худшее для платящих.
  Фикс: регулярный pg_dump/экспорт в отдельное место + ОДНА репетиция восстановления.
- ⚪ Нет staging/preview (пуш в main = прод); нет uptime-мониторинга/алертинга;
  откат — только средствами Vercel (dashboard) + git revert. Правило «сначала SQL,
  потом код» держится на дисциплине (текст в CLAUDE.md), не на инструменте.

### Действие владельца (не код)

- ⚪ **Пилот спланирован, но не запущен**: скрипт сообщения репетиторам
  (docs/tg-launch.md) и бартер на 3 места (monetization-draft) готовы.
  Отправить сообщения, добавить 2–3 репетиторов в белый список. Самый дешёвый
  следующий шаг проверки спроса (глава 2 учебника).
- ⚪ **handoff/ в корне** — папка с макетом Nocturne (частично в гите). Дубль
  handoff/LoginPage.tsx может путать. Оставлена как референс дизайна; убрать
  или нет — на усмотрение владельца (риск случайного коммита закрыт: standalone
  html теперь в .gitignore).

### Соседние места (проверка по классам багов захода 21, 2026-07-27) — записано, НЕ чинить

Принцип «рядом с багом живут другие». Аудит соседей по двум классам. Класс
«RPC-only не зафорсен» (как был study_plans) — **чисто, нового брата нет**:
у material_assignments/word_checks/grammar_quests/placement_requests/activity_log
revoke на месте. Класс «jsonb/text без лимита размера» дал список (всё 🟡/⚪ —
это раздувание общей БД одним аккаунтом, не утечка и не подделка балла):

**Прямая запись клиента (RLS for all, ни revoke, ни CHECK длины):**
- 🟡 `profiles.display_name`, `decks.title`/`description` — любой аккаунт одним
  REST-запросом кладёт строку любой длины. Добавить CHECK char_length.
- 🟡 `writing_submissions.feedback` (jsonb) — клиент пишет AI-фидбек сам, размер
  не ограничен (text/prompt уже закрыты, feedback пропущен).
- ⚪ `profiles.native_lang`; `cards.ipa`/`audio_url` (cards_len_check покрыл
  front/back/example, эти два пропустил); `materials.topic/format/length_range/
  title` (size-CHECK только на body/exercises/plan).

**Через security-definer RPC, но без pg_column_size-гарда внутри:**
- 🟡 `material_assignments.answers` ← submit_material (пишет УЧЕНИЦА — широкий
  вектор); `word_checks.results` ← submit_word_check (ученица). Добавить
  pg_column_size-проверку в RPC (как в save_quest_messages/set_daily_plan).
- ⚪ `material_assignments.ai_review`/`teacher_review` (учитель); `word_checks.
  card_ids` (число id не ограничено); `study_plans.goal/summary/level` (weeks
  капнут, эти — нет; level ещё и без enum-CHECK).

**Наблюдения (не эксплойт, но стоит знать):**
- ⚪ Мёртвые for-update политики «student updates …» на material_assignments и
  word_checks обезврежены только `revoke update`. Если кто-то снимет revoke
  «чтобы починить» — дыра откроется молча. Удалить политики или пометить.
- ⚪ **Флуд числом строк** (третий класс, сосед grammar_mistakes-капа): decks,
  cards, conversations, messages, writing_submissions, review_states — for all
  без потолка строк на пользователя. Один аккаунт нагенерит сколько угодно.
  Защищаться — если/когда откроемся публично.

**Клиентский аудит соседей (класс «клиент считает доверенное значение»):**
- 🔴 **word_checks: результат перепроверки считается на клиенте, сервер не
  пересчитывает** — ✔ ПОДТВЕРЖДЕНО чтением кода. WordCheckRunner.tsx:40,52
  считает `ok = answerMatches(...)` в браузере; submitWordCheck шлёт results как
  есть; RPC submit_word_check (schema.sql:616) делает `set results = p_results`
  БЕЗ пересчёта. Результат читает ПРЕПОДАВАТЕЛЬ (getWordChecks →
  StudentWordsSection) — ученица через DevTools вызывает RPC с `ok:true` по всем
  словам → «перепроверка пройдена идеально» + FSNS-штраф `again` тоже не
  начисляется. Это ТОЧНАЯ параллель review_states/activity_log, но ХУЖЕ (третья
  сторона доверяет сигналу), и прямой контраст с materials.auto_score, который
  СПЕЦИАЛЬНО пересчитывают на сервере. Фикс (тот же класс, что review_states —
  чинить ДО чужих учениц): пересчёт `ok` в submit_word_check по cards.front
  (у RPC есть card_ids + доступ к cards). Сгруппировать с review_states.
- 🟡 **placement_requests: уровень теста — самоотчёт без проверки** (ТОЧНО
  структурно). submit_placement (schema.sql:2120) валидирует только, что p_level
  — валидный CEFR, но ответы теста на сервер не идут. Ученица шлёт `level:'C2'`
  без единого верного ответа → преподаватель видит фальшивый уровень, он же идёт
  в промпты AI и диагностику. Мягче word_checks (вредит в основном себе —
  контент не по уровню), но класс тот же. Полный фикс дорог (считать тест на
  сервере); приемлемо оставить до открытия для чужих.
- Клиентский аудит подтвердил БЕЗОПАСНЫМИ: conversations/messages (подделка
  role='assistant' видна только себе), deck_assignments (RLS: своя колода + своя
  ученица), profiles.update (грант на 3 колонки), contextDict (шлюз
  cards_len_check), studyPlan.sanitize (реально покрывает типы/topicId/длины),
  cards.source (влияет только на бейдж).

## Закрыто

- ✅ 2026-08-08 — 🔴 **ЭТАП 2 плана ремонта: деньги и честность отказов.**
  Схема залита владельцем, прод проверен. Смоуки после заливки: refund 15/15,
  energy 27/27, quota-seats 12/12, zahod21 10/10, profile-leak 16/16,
  atomic 5/5, diagnostics 7/7, program 11/11, dailyplan 6/6, navigation 24/24,
  features 19/19, ux-audit 0, прод smoke-aitasks 6/6.
  - *Энергия сгорала, когда AI не отвечал.* Списание шло ДО обращения к модели:
    при выгорании всей цепочки (у Google суточные лимиты общие на проект)
    человек терял ⚡ и получал отказ — платил за чужую активность. Теперь
    правило «не доставили ответ — не берём плату», и для «Речи» тоже.
    **Почему через токен:** сервер ходит в базу под токеном пользователя, и
    RPC «верни последнее списание» была бы доступна ему самому из браузера —
    безлимитный AI в один вызов. Списание метится неугадываемым токеном,
    который рождается на сервере и клиенту не уходит.
  - *Репетитор на первой генерации читал «Лимит исчерпан».* Было 0 генераций —
    сообщение вдобавок неправда. Стало 2 (ровно один материал: план + текст),
    плюс счётчик остатка в «Материалах» ДО того, как в лимит упрёшься.
  - *«TypeError: Failed to fetch» вместо человеческой ошибки* — 28 мест
    переведены на общую `lib/dbError.ts`. Наши RPC, отвечающие по-русски и по
    делу, пропускаются как есть: слепая замена была бы регрессией.
  - Тексты: убрано «это бывает на бесплатном тарифе» (читал платящий), код
    ответа наружу и имя переменной `GROQ_API_KEY`.
  **Не проверено (честно):** возврат энергии end-to-end на настоящем отказе
  поставщика — принудительно выжечь суточную квоту Google нельзя. Проверены
  обе половины по отдельности: RPC возврата (15/15, включая 6 проверок на
  злоупотребление) и что сервер зовёт её на всех путях отказа (по коду).

- ✅ 2026-08-07 — 🔴 **ЭТАП 1 плана ремонта: приложение научилось «назад».**
  Приёмка — новый постоянный смоук `scripts/smoke-navigation.mjs`: было 9/13,
  стало 13/13. `ux-audit` 16 экранов — 0 замечаний (регрессий нет).
  - *Внутренние экраны — в адрес.* Общий хук `src/lib/useUrlState.ts` (там же
    правило: заход внутрь пушит запись в историю, возврат кареткой — нет).
    Переведены: читалки EN и ES, «Мои тексты», хаб «Учёбы» и подэкран «Слова»,
    уроки грамматики и все три раздела глаголов, вкладка студии, открытый
    материал, задания и квесты. Адреса стали говорящими:
    `/study?view=reader&text=b1-habits`, `/grammar?lesson=0`. Побочный
    выигрыш: у текста появилась ссылка — преподаватель может прислать
    «прочитай вот это».
  - *«Начать первое занятие» вело в меню.* Решение, куда вести, принимается
    ДО перехода (`startGuidedRoute`). Тот же баг был и на кнопке с Главной —
    починен класс, а не один экран.
  - *«Методичка» была недоступна на 390px.* Ряд вкладок переносится на вторую
    строку. Замер после правки: все 4 вкладки внутри экрана, высота 44px,
    горизонтальной прокрутки у страницы нет. Прокрутку не выбрали намеренно:
    она убрала бы обрезку, но человек по-прежнему не знал бы о четвёртой
    вкладке.
  - *Упражнения задания без заголовка и возврата* — добавлен BackHeader,
    ответы переживают уход и возврат.
  **Три ловушки, пойманные по ходу** (записаны в коде, чтобы не повторить):
  эффект «сбросить при смене языка» срабатывает и на первом рендере и стирал
  бы параметр из прямой ссылки — сравнивать надо ЗНАЧЕНИЕ через ref, а не
  считать прогоны (в StrictMode их два); в материалах эффект встал выше
  объявления данных; сам смоук навигации дважды врал в пользу продукта
  (сравнение по адресу проходит всегда, пока состояние не в адресе).
  **Сознательно НЕ в адресе:** ход раунда игры (после F5 честно начинается
  заново) и шаги мастера генерации материала (держат в памяти ответ AI —
  ссылка открывала бы пустой мастер).

- ✅ 2026-08-07 — 🔴 **Четыре неправды и англоязычный вход (находки ревью 2В).**
  - *Экран входа отвечал по-английски.* `AuthContext` отдавал `error.message`
    как есть: неверный пароль → «Invalid login credentials». Это первое, что
    видит новый человек, промахнувшийся по клавише. Добавлена
    `describeAuthError()` в `lib/access.ts` — переводит известные ответы GoTrue
    (неверные данные, неподтверждённая почта, короткий пароль, занятый адрес,
    кривой формат, частые попытки, закрытая регистрация, нет сети), незнакомое
    отдаёт с припиской «попробуй ещё раз, если повторится — напиши нам».
    `describeSignUpError` теперь тоже проходит через неё.
  - *«Минимум 6 символов» под полем пароля* при требовании базы в 8 — человек
    делал ровно то, что мы просим, и получал отказ. Подпись и `minLength` → 8.
  - *Карточки тарифов обещали «переводы и произношение без лимитов».* Утренняя
    правка (`23d84ad`) прошла по подписи и оферте, но сами пункты карточек в
    `lib/billing.ts` пропустила — та же неправда осталась на самом видном месте.
    Проставлены реальные числа: 100/50 на Free, 900/400 на Premium.
  - *Пункт A4: «До 25 AI-материалов в месяц».* Лимит считает вызовы Pro-модели,
    а материал стоит ДВУХ (план + текст) — реальных материалов ~12. Туда же
    тихо уходят задания «Письма» (`lib/writing.ts:238,267`). Формулировка
    честная: «25 AI-генераций — это ~12 материалов», и так по всем трём тарифам.
  **Урок тот же, что этажом ниже, только жёстче:** правку лимитов мало провести
  по оферте — обещания живут ещё и в `lib/billing.ts`, и в подписях форм.

- ✅ 2026-08-07 — 🔴 **Оферта и «Тарифы» обещали больше, чем даёт код.** Коммит
  `a07a6ad` того же дня ввёл ступеньку триала (30 ⚡ первые три дня, дальше 15) и
  срезал переводы 300→150, но правил только `schema.sql`. В текстах остались
  «30 ⚡ в день» на весь пробный период и «перевод и произношение без лимита
  всегда» — при том, что суточные капы у них есть и с внятной ошибкой
  (`RECALL_LIGHT_LIMIT` / `RECALL_SPEECH_LIMIT`). Для страницы тарифов это
  завышенное обещание, для `/terms` — расхождение с публичной офертой.
  Переписаны оба текста по фактическим числам из `spend_energy`: энергия
  5 / 30 / 30→15, переводы 900 / 150 / 100, произношение 400 / 150 / 50,
  карточки-игры-грамматика действительно без ограничений.
  **Урок:** правка лимитов в схеме обязана в том же заходе проходить по
  `PricingPage.tsx` и `LegalPage.tsx` — это часть изменения, а не документация.

- ✅ 2026-07-27 — **Заход 21 «Закрытие находок аудита»** (⚠️ SQL-блок «ЗАХОД 21»
  в schema.sql надо ВЫПОЛНИТЬ в Supabase перед пушем):
  - 🔴 #11 **Анонимный оракул кодов-приглашений + системная дыра грантов** —
    `revoke execute on all functions from public` + `grant … to authenticated`
    (закрыло вызов RPC анонимом системно), join_teacher пересоздан с проверкой
    входа первой строкой. Проверено: единственный анонимный вызов
    (get_my_plan на /pricing) глотает ошибку → страница работает.
  - 🟡 #13 **activity_log** — RPC `log_activity(type, day, …)`: день клиента
    принимается только ±1 сутки от серверной даты (стрик нельзя дорисовать
    задним числом), type валидируется, счётчики клампятся; прямая запись
    отозвана. Клиент (lib/activity.ts) переведён на RPC.
  - 🟡 #14 **localStorage без границы между аккаунтами** — signOut теперь
    стирает ВСЕ пользовательские ключи recall.* (кроме настроек устройства:
    lang, settings, chunk_reload_at). lib/profile.ts clearUserLocalData.
    ⚠️ Трейд-офф (ревью Opus): es_level и my_texts — localStorage-only без
    серверной копии, при выходе теряются (на личном устройстве — перепройти
    placement / заново вставить тексты). Приемлемо (выход редок, приватность
    важнее), правильный фикс — ключи с постфиксом .${userId} — сделать в
    заходе про lib/storage.ts (см. «Крупные рефакторинги» выше).
  - 🟡 #15 **materials** — политика требует role='teacher' + лимит размера
    (pg_column_size < 100КБ на body/exercises/plan).
  - 🟡 #16 **study_plans** — `revoke insert/update/delete` (replace_study_plan
    стала обязательной, гонка закрыта).
  - 🟡 #17 **Лимиты длины** cards/messages/writing_submissions (CHECK … NOT
    VALID — только новые записи, старые данные не трогаются).
  - ⚪ #18 **grammar_mistakes** — триггер-потолок 5000 строк на пользователя.
  - 🟡 #4 **batchWords без валидации ответа AI** — parseTranslated приводит
    каждый элемент к строкам (мусор Gemini больше не ляжет в карточки).
  - 🟡 #7 **Мёртвый spanishDict.ts** удалён (0 импортов), ссылки в
    ARCHITECTURE.md поправлены на contextDict.
  - 🟡 #1 **Лендинг /teachers** — в CTA добавлена пометка «сейчас доступ по
    приглашению» (посторонний не упрётся в тупик).
  - 🟡 #10 **Мусор в корне** — «Recall Приложение (standalone).html» добавлен
    в .gitignore (риск случайного коммита закрыт).
- ✅ 2026-07-27 — **#5 бог-файл MaterialsSection** (805 строк, 6 компонентов) →
  оркестратор 180 строк + features/teacher/materials/{RequestForm,PlanScreen,
  PreviewScreen,MaterialDetail,MaterialsByLevel,shared}. Поведение не изменено.
- ✅ 2026-07-27 — **#6 хрупкие non-null + noUncheckedIndexedAccess**: флаг
  включён в tsconfig.app.json; всплывшие 109 ошибок индексации устранены по
  всему проекту (guard'ы, повторяющие обработку конца раунда; `?? fallback`;
  `!` только с явной гарантией границ и комментарием — свопы Фишера-Йейтса,
  модульная индексация). Работа распараллелена (2 агента + lib сам), сведена
  централизованно. Заодно починен ранее сломанный test-distractors (recentWords
  → storage.ts с явным расширением для node-стрипа типов). Проверка: tsc 0,
  build, 5 юнит-тестов чистых модулей, ux-audit 16 экранов 0 JS-ошибок,
  smoke-features/phrasal (стэш-сравнение подтвердило: 3 флаки-ассерта свежего
  аккаунта — предсуществующие, не регрессия).
- ✅ 2026-07-27 — **#3 Supabase-клиент без типа схемы** (флагман аудита):
  сгенерированы типы `src/lib/database.types.ts` (`supabase gen types`, вариант А
  — токен владельца), клиент → `createClient<Database>`. Всплыло 15 реальных
  расхождений код↔база, все устранены по природе: patch'и профиля/карточки
  типизированы `TablesUpdate<>` (реальный выигрыш), запись структур в jsonb/
  аргументы RPC — мост `toJson()`, возвраты RPC (json) — явный `as unknown as`,
  строки study_plans — точечный маппер rowToPlan (сохраняет проверку 8 колонок,
  явно кастует только jsonb/text-поля). Теперь переименование колонки в БД
  ловится на сборке. Проверка: tsc 0 ошибок, build + смоуки (program/quests/
  dailyplan/diagnostics/zahod21) зелёные. ⚠️ После правок schema.sql —
  перегенерировать типы (см. ARCHITECTURE §5).
- ✅ 2026-07-27 — **#8 общий lib/storage.ts + #9 localStorage из компонентов**:
  создан lib/storage.ts (readJson/writeJson/readRaw/writeRaw — единая безопасная
  обёртка); на него переведены 13 модулей (mistakes, recentWords, myTexts,
  gameMisses, verbMistakes, esLevel, onboarding, settings, definitions, profile,
  wordPool, studyPlan, LanguageContext) — поведение сохранено. Ключ
  program_seen централизован (isProgramSeen/markProgramSeen в studyPlan),
  туториал-флаг DeckReview и язык переведены на storage. Прямой localStorage в
  src остался только в clearUserLocalData (перечисление ключей). Проверка:
  build + ux-audit 16 экранов, 0 JS-ошибок (3 известных inline-исключения «Речи»).
- ✅ 2026-07-26 — **docs/ARCHITECTURE.md устарел ~наполовину** (находка У2):
  переписан целиком и синхронизирован с кодом; добавлено правило «обновлять в
  тот же заход, что меняет структуру».
