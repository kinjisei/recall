# План заходов Recall (обновлён 2026-07-23)

> Каждый заход = один промпт ниже. Копируй промпт целиком в новую сессию
> Claude Code. Порядок рекомендуемый, но можно менять.
> Выполненные заходы помечай ✅ прямо здесь.

## Статус

| # | Заход | Статус |
|---|---|---|
| 1 | Мелкие фиксы UX (уровень, балл, материалы, повторы слов) | ✅ в проде |
| 2 | Слова у учителя (просмотр + выборка) | ✅ в проде |
| 3 | Фразовые глаголы (60 глаголов, 476 фраз) | ✅ задеплоено 23.07 |
| 4 | Идиомы из EFE (911 шт., 20 тем) | ✅ задеплоено 23.07 |
| 5 | Методичка преподавателя | ✅ задеплоено 23.07 |
| 6 | Учитель-плюс (уведомление, динамика, PDF-отчёт) | ✅ в проде |
| 7 | OCR сканов → gap-отчёт 2.0 (53 пробела) | ✅ задеплоено 23.07 |
| Л | Лендинг /teachers | ✅ задеплоено 23.07 |
| 8 | Полировка экранов (анимации, квесты, диалог) | ⬜ следующий |
| 9 | Игры: умные обманки + Спринт-антиповтор | ⬜ |
| 10 | План дня от учителя + стрик (сначала вопросы!) | ⬜ |
| 11+12 | «База уровня» из 4000 Essential Words | ⬜ |
| 13 | Свои тексты ученика (вставка/PDF/DOCX) | ⬜ |
| 14 | Мультивыбор слов в читалке | ⬜ |
| — | Новые уроки грамматики по gap-отчёту (топ-10) | ⬜ когда решим |
| — | Открытие регистрации (SQL готов) + уборка «закрытый тест» | ⬜ перед запуском |

---

## Заход 8 — Полировка экранов

```
Роль: ты ведущий разработчик Recall (CLAUDE.md, рабочие принципы). Прочитай скилл ui-ux-pro-max (тач/анимации) перед правками.
5 фиксов, каждый — во всём коде, не точечно:
1. «Учёба»: строки «Задания от преподавателя» и «AI-квесты» приходят из async-запросов позже остальных и вываливаются вразнобой без анимации. Сделай: собери все async-источники хаба в Promise.all → рендери ВЕСЬ список строк разом со stagger-анимацией (animate-fade-up с задержками по индексу, как на Главной); пока грузится — лёгкие скелетоны той же высоты (без прыжков вёрстки).
2. «Твой уровень» всё ещё с задержкой: добавь мгновенный localStorage-кэш уровня EN (recall.en_level_cache) — строка рендерится сразу из кэша, обновляется тихо после ответа профиля; кэш обновлять при каждом чтении профиля и сбрасывать при выходе.
3. AI-квесты (/quests): экран открывается «не сверху», нет заголовка и кнопки «Назад». Сделай как в «Диалоге»: BackHeader с названием квеста сверху, ScrollToTop при входе, поле ввода ЗАКРЕПЛЕНО снизу (паттерн useKeyboardInset из ConversationPage — при клавиатуре ничего не «уезжает наверх»), сообщения скроллятся к ПОСЛЕДНЕМУ автоматически.
4. «Диалог»: автоскролл к последнему сообщению при каждом новом (как в мессенджерах, scrollIntoView по последнему элементу); проверь, что шапка приложения (Recall · EN/ES) остаётся видимой — на телефоне она пропадает.
5. Пройди ВСЕ экраны с чатами/списками на предмет «открывается не сверху»: грепни отсутствие ScrollToTop/window.scrollTo в роутах (assignments, program, grammar-топик уже был). Почини найденное.
Критерии: build; node scripts/ux-audit.mjs без новых замечаний; скриншоты «Учёбы» с заданиями (создать тестовую пару учитель+ученица с заданием и квестом) и /quests — посмотреть глазами. Коммит без пуша.
```

## Заход 9 — Игры: умные обманки + Спринт

```
Роль: ты ведущий разработчик Recall (CLAUDE.md). Жалоба владельца: в играх слов повторы («босс» дважды, при новом запуске те же слова) и ОЧЕВИДНЫЕ ответы (незнакомое слово угадывается исключением).
1. lib/wordPool.ts: новый экспорт pickDistractors(correct, pool, n): обманки ранжируются по похожести на правильный ответ — та же тема (topic), тот же уровень, та же часть речи (эвристика по окончанию русского перевода: -ть=глагол, -ый/-ой/-ий=прилагательное, иначе существительное), близкая длина перевода (±40%). Верный ответ не должен быть самым длинным/коротким в четвёрке — если выделяется, замени обманки.
2. Подключи pickDistractors во ВСЕ режимы с вариантами: Перевод, Значения (Match), Пропуск, Аудирование (QuizModes/MatchMode — сейчас обманки случайные).
3. SprintMode подключи к анти-повтору (withoutRecent/recordShown из wordPool) — в прошлый раз он был сознательно пропущен, это ошибка.
4. Проверь, что анти-повтор реально работает во всех режимах подряд: мини-тест на pickWords+recordShown (node, стрип типов) — два раунда подряд не пересекаются, пока пул позволяет.
Критерии: build; тест; ручная проверка: 3 раунда «Перевода» подряд — слова не повторяются, обманки одной части речи. Коммит без пуша.
```

## Заход 10 — План дня от учителя + стрик (сначала вопросы!)

```
Роль: ты продакт+разработчик Recall (CLAUDE.md). НЕ ПИШИ КОД сразу — сначала предложи дизайн и задай вопросы владельцу.
Контекст: сейчас стрик засчитывается за ЛЮБОЕ одно занятие (activity_log), а «План на сегодня» на Главной — фиксированные 4 пункта (слова/чтение/речь/диалог). Новый функционал (задания, квесты, программа) в план не входит.
Идея владельца: учитель сам выбирает ежедневные задания для ученицы; если не выбрал — дефолтный набор наш.
Проработай: (1) что включить в дефолтный план с учётом всего функционала (слова FSRS обязательны — это ядро; +1-2 ротируемых пункта?); (2) как учитель задаёт план (в карточке ученицы? чекбоксы из списка типов активности + материал/квест?); (3) стрик: за 1 пункт как сейчас, за все пункты, или за N из M — и как не сломать существующие стрики учениц; (4) схема БД (таблица daily_plans или jsonb в teacher_students) + RLS.
Выдай: 2-3 варианта с рекомендацией, вопросы владельцу, и только после его ответов — код со смоуком.
```

## Заходы 11+12 — «База уровня» из 4000 Essential Words

```
Роль: ты контент-инженер Recall (CLAUDE.md). Владелец одобрил multi-agent workflow. pymupdf установлен, путь vision-конвейера проверен на идиомах.
Источник: D:\Downloads\sounds\1.pdf … 6.pdf — «4000 Essential English Words 1-6» (Paul Nation): каждый юнит = текст → ~20 целевых слов с определениями и примерами → однотипные упражнения.
Задача А (слова): отрендерить страницы всех 6 томов в PNG (pymupdf, dpi 110) → vision-агенты (Haiku) по 6-8 страниц выписывают ЦЕЛЕВЫЕ слова юнитов: word, определение EN, пример EN, том, юнит → Sonnet: перевод на русский, чистка; уровни по томам: 1→A2, 2→A2-B1, 3→B1, 4→B1-B2, 5→B2, 6→B2-C1. Тексты книги НЕ сохранять (копирайт).
Задача Б (структура паков): новая группа тем «База уровня» — «База A2: самые нужные слова» и т.д. (это «минимальный словарь чтобы разъясняться»: 4000 Essential Words — частотный минимум по Нейшену). В PacksSheet паки делятся на категории: «База уровня» / «Темы» / «Идиомы» (сворачиваемые группы).
Задача В (упражнения): посмотреть 3-4 юнита глазами — подтвердить, что упражнения однотипные; если да — описать их шаблоны в docs/ (для будущего генератора), сами упражнения НЕ переносить.
Проверки: сверка 20 случайных слов со страницами книги (vision), валидатор данных, смоук паков (пак «База» добавляется в колоду), build. Коммит без пуша.
```

## Заход 13 — Свои тексты ученика

```
Роль: ты ведущий разработчик Recall (CLAUDE.md).
Фича: ученик добавляет СВОЙ текст в читалку. Кнопка «+ Свой текст» в «Учёба → Тексты и диалоги»: (1) вставка текста в textarea; (2) загрузка PDF (pdfjs-dist, извлечение текста на клиенте); (3) DOCX (mammoth); (4) TXT. Лимит 15 000 знаков (обрезка с предупреждением). Хранение в localStorage (recall.my_texts.<lang>, максимум ~10 текстов — чужой копирайт не попадает в нашу БД); список «Мои тексты» над встроенными, удаление кнопкой. Текст открывается в обычной читалке: тап по слову → контекстный словарь → «В колоду» (переиспользовать ReaderView).
Ограничения: новые библиотеки — ленивыми чанками (pdfjs тяжёлый, грузить только при выборе PDF); размер стартового бандла не должен вырасти.
Критерии: build; смоук: вставить текст → тап по слову → слово в колоде; PDF с текстовым слоем извлекается. Коммит без пуша.
```

## Заход 14 — Мультивыбор слов в читалке

```
Роль: ты ведущий разработчик Recall (CLAUDE.md). Прочитай frontend-design перед вёрсткой.
Фича: режим «Отметить слова» в читалке (EN/ES/задания — везде, где WordSheet). Кнопка-тумблер в шапке читалки → тапы по словам подсвечивают их (повторный тап снимает), снизу плавающая панель «Добавить в колоду (N)» + «Отмена». Добавление: для каждого слова — базовая форма и перевод пакетно через контекстный словарь (tier lite, батч-запрос ОДНИМ промптом на все слова — не по запросу на слово, побереги квоту), затем addCardsBulk с примерами-предложениями. Обычный тап вне режима работает как раньше (шторка одного слова).
Критерии: build; ux-audit; смоук: отметить 3 слова → в колоде 3 карточки с переводами. Коммит без пуша.
```

## Внеочередное (по команде владельца)

- **Открытие регистрации**: выполнить блок «ОТКРЫТАЯ РЕГИСТРАЦИЯ» из schema.sql
  → сказать Claude «регистрация открыта» → он уберёт «закрытый тест» с /login
  и задеплоит. Делать прямо перед публичным постом.
- **Новые уроки грамматики**: топ-10 из docs/grammar-gap-report.md (первые
  кандидаты: Wh-вопросы A1, наречия частотности A1, countable/uncountable A2,
  -ed/-ing прилагательные B1, be/get used to B1).
- **NotebookLM-промпт** (если понадобится вытащить что-то ещё):
  «Извлеки из источника ПОЛНЫЙ список целевых слов юнитов — все юниты с первого
  до последнего. Формат — только markdown-таблица: | Юнит | Слово | Часть речи |
  Определение (EN) | Пример (EN) |. Не сокращай и не группируй. Если ответ не
  влезает — закончи строкой "ПРОДОЛЖЕНИЕ: юнит N", я скажу "продолжай"».

---

# Новые заходы (24.07) — от сложного к простому

## Заход 15 — Полное ревью 22 июля (монетизация, диагностика, программа)

```
Роль: ты аудитор Recall (CLAUDE.md, принципы владельца). Владелец одобрил multi-agent workflow.
Задача: полное ревью изменений за 22 июля — диапазон коммитов от «Монетизация v1» до e8a7f0b включительно (git log --since/--until или по хешам). Области: тарифные RPC (consume_ai_quota старый/новый, get_my_plan, admin_*, has_premium_access/has_paid_access — согласованность после блока «КВОТА ТРИАЛА»), /api/gemini и _auth.ts (обход квоты, подмена tier, невалиданные входы), PricingPage/AdminPage, блоки ДИАГНОСТИКА и ПРОГРАММА (RLS, лимиты), клиенты lib/billing, lib/admin.
Метод: 4-5 Opus-файндеров по областям → каждую находку опровергают 2 Sonnet-скептика → подтверждённое чинить сразу (найдя дыру — проверь соседние места с тем же паттерном!), спорное — вопросом владельцу.
Критерии: build, затронутые смоуки перепрогнаны, отчёт: что нашли/починили/что решать владельцу. Коммит без пуша.
```

## Заход 16 — Ревью документации и всех текстов + humanizer

```
Роль: ты редактор и скептик Recall (CLAUDE.md). Установи скилл humanizer (убирает тон ИИ; ищи в реестрах awesome-skills/маркетплейсах; если не найдётся — возьми ближайший по смыслу или напиши свой через skill-creator по правилам: без канцелярита, без «мы рады представить», без списков-из-трёх, живые формулировки).
Задача из двух частей.
1. СОМНЕВАЙСЯ ВО ВСЁМ: пройди /terms, /privacy, /pricing, /teachers, тексты онбординга и методички как въедливый юрист+клиент: несостыковки с реальным поведением приложения (лимиты, триал 12/день, Kaspi, «ученицам бесплатно»), обещания, которые мы не выполняем, юридические дыры (возвраты, персональные данные детей — ученицы могут быть несовершеннолетними!, прекращение сервиса). Что можешь чинить сам — чини, где нужен выбор — спрашивай владельца.
2. Перепиши тексты humanizer-ом: лендинг /teachers, /pricing, тексты Login/онбординга. Термины — человеческим языком: «алгоритм FSRS» → «приложение само напоминает повторить слово именно тогда, когда оно начинает забываться»; «AI-действие» → объяснение в одну строку рядом с лимитом. Цифры лимитов: триал 12/день, платный 200/день, free 5/день — единообразно ВЕЗДЕ.
Критерии: build; вычитка глазами (скриншоты лендинга/прайсинга); список юридических вопросов владельцу отдельным блоком. Коммит без пуша.
```

## Заход 17 — Все траты проекта + биллинг Google AI Studio (ресёрч)

```
Роль: ты финансовый аналитик Recall. Владелец одобрил multi-agent workflow (Sonnet-ресёрчеры с WebSearch, факты только из источников).
Задача: (1) полная карта текущих и будущих трат: Vercel (Hobby→Pro: когда обязаны перейти, цена), Supabase (free-лимиты: БД 500МБ, MAU, когда упрёмся с N учениц), Google AI Studio/Gemini API (см. п.2), Groq (free-лимиты), домен, Google Play $25, налоги самозанятого ~4%; свести в таблицу «при 0 / 50 / 200 / 1000 пользователях».
(2) ГЛАВНЫЙ вопрос владельца про Gemini: что именно происходит с бесплатными лимитами при подключении billing к проекту — остаётся ли free tier, или включение биллинга переводит ключ на платный тариф без бесплатных дневных квот? Можно ли иметь два проекта (ключ бесплатный + ключ платный) и мягко переключаться? Только первоисточники (ai.google.dev/pricing, документация, официальные форумы), с цитатами и датами.
Выход: docs/costs.md с таблицей и ответами + рекомендация действий. Коммит без пуша.
```

## Заход 18 — Проверка «дыры повсюду» в свежих фичах 24.07

```
Роль: ты пентестер+QA Recall (CLAUDE.CLAUDE.md, принципы). Найдя дыру — сразу ищи ту же по соседству.
Цели (свежий код, ревью его касалось поверхностно): quota-обход в новых фичах (batchWords — считается ли батч-перевод ОДНИМ действием и можно ли гонять его в цикле по 15 слов дёшево? consume_ai_quota на каждый /api/gemini — значит 1 батч = 1 действие: осознать и решить с владельцем, честно ли), MarkableText (XSS через свой текст? текст рендерится как текст — проверить), myTexts (переполнение localStorage, кривые PDF), смоуки-скрипты в репо (не светят ли секреты — .env.local читается, но не коммитится ли вывод), лендинг (утечка внутренних цифр?).
Критерии: build; отчёт находок с фиксами; вопросы владельцу. Коммит без пуша.
```

## Заход 19 — Открытие регистрации (когда решишь запускаться)

```
Чек-лист (выполнять по порядку, в одной сессии с Claude):
1. Supabase Dashboard → Authentication → Providers → Email → включить Confirm email.
2. SQL Editor → выполнить блок «КВОТА ТРИАЛА» (если ещё не выполнен) → затем блок «ОТКРЫТАЯ РЕГИСТРАЦИЯ».
3. Сказать Claude: «регистрация открыта» — он уберёт «закрытый тест» с /login, проверит смоуком регистрацию нового ящика и задеплоит.
4. Только после этого — пост в TG.
```

## Заход 20 — Утечка профиля преподавателя (нужен выбор владельца)

```
Находка аудита 22.07 (подтверждена): политика «linked profiles visible» отдаёт
ученице ВСЮ строку профиля её преподавателя — включая invite_code, plan,
plan_expires_at, is_admin. Ученица через DevTools делает
supabase.from('profiles').select('*').eq('id', <teacher_id>) и получает код
приглашения; дальше код можно опубликовать где угодно. Лимит мест (добавлен
24.07) ущерб ограничивает, но чужие люди всё равно займут места ученицы.
Плюс invite_code нельзя перевыпустить — ensure_invite_code всегда возвращает
существующий.
Задача: (1) закрыть чувствительные поля — перевести чтение профилей связанных
сторон на security-definer RPC/представление, отдающее только id, display_name,
level, role (клиенты: lib/teacher.ts getMyTeachers/getMyStudents,
lib/profile.ts, TeacherPage); (2) добавить RPC regenerate_invite_code (кнопка
«Сменить код» у преподавателя); (3) проверить соседние политики на такую же
утечку (teacher_students, decks, cards, review_states — что ещё отдаётся
целой строкой не тому). Смоук: ученица НЕ видит invite_code/plan учителя.
Критерии: build, смоуки, коммит без пуша.
```
