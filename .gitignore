node_modules
dist
dist-ssr
*.tsbuildinfo
*.local
.env.local
.env
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*

# архивы (не исходники — и ломают файловый watcher Vite, если открыты)
*.zip

# отчёт UX-аудита (генерируется scripts/ux-audit.mjs при каждом прогоне)
ux-audit-report.md

# прототипы Omelette (референсы дизайна, не исходники)
*.dc.html
# Android TWA: сгенерированный проект и ключи не коммитим (ключ — секрет)
twa/*
!twa/twa-manifest.json
