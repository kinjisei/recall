# Recall — контекст проекта для Claude

> Этот файл читается автоматически при старте сессии Claude Code в этой папке.
> Он даёт полный контекст, чтобы продолжить работу без потери нити.

## Что это
**Recall** — PWA для изучения **двух языков: английского и испанского**
(переключатель EN/ES в шапке; язык хранится в localStorage `recall.lang`).
- Пользователь (владелец): англ. был B2, сейчас ~B1, цель — восстановить; испанский — с нуля.
- Его девушка: англ. C1, цель — не терять навык.
- Будущее (Фаза 4): режим «Преподаватель» — девушка учит своих учениц через приложение.
- Условие: всё на **бесплатных** тарифах.

2026-07-07: в Recall вливается контент Flutter-приложения `d:\projects\spanish`.
Flutter-код не переносится (другой стек), приложение spanish остаётся нетронутым
как источник. Перенос по частям — по фиче за сессию.
Уже перенесено: 40 текстов, 25 диалогов, 105 фраз произношения (`src/data/spanish/*.json`);
**весь словарь** — все паки A1–B2, ~4668 слов в ~281 теме, включая частотный CSV
(`src/data/spanish/words/` + ленивый `words.ts`, отдельный чанк ~856 КБ; «Паки» в Колоде
с группировкой по уровням и поиском);
**грамматика** — экран `features/grammar/GrammarPage.tsx`, два раздела:
  • «📖 Уроки» — 74 урока A1–B2 (`src/data/spanish/grammar/` + ленивый `grammar.ts`):
    теория (paragraph/table/example) + упражнения (mcq/fill/order) с проверкой;
  • «🔀 Глаголы» (`features/grammar/ConjugationSection.tsx` + ленивый
    `src/data/spanish/conjugation.ts`): «Справочник» — 14 времён (окончания -AR/-ER/-IR
    + таблицы примеров глаголов) и «Тренажёр» — 110 упражнений на выбор формы с объяснением.
  Вкладка «📚 Грам.» и карточка на дашборде — только в ES; итог упражнений → стрик
  (`logActivity('grammar')`). Грамматика в БД не пишет (только activity_log).
**практика** — `features/practice/` (хаб PracticePage + 4 мини-игры: MatchGame,
  TranslationDrill, SentenceBuilder, ListeningGame; `util.ts` — shuffle/sample/пул слов).
  Игры переиспользуют перенесённые слова (ленивый пул) и фразы (`sentences.json`);
  новых данных не требуют. Карточка «🎮 Практика» на дашборде — только в ES; завершение
  раунда → стрик (`logActivity('practice')`). Роут `/practice`, в БД не пишет.
**placement-тест** — `features/onboarding/PlacementTest.tsx` (роут `/placement`,
  ленивый `src/data/spanish/placement.ts`, 48 вопросов A1–B2, по 5 на уровень).
  Результат — уровень испанского — хранится в localStorage (`lib/esLevel.ts`,
  ключ `recall.es_level`), НЕ в БД. Используется в «Диалоге» (промпт под уровень)
  и на дашборде. Карточка «Определи уровень» — в ES, пока уровень не задан.
**ВЕСЬ учебный материал spanish перенесён.** Осталась только Flutter-специфика
  (нативные уведомления, msix), которая в веб-PWA не нужна.

Дизайн-система (2026-07-07): единый пласт — токены и базовые стили в `src/index.css`
  (акцент sky + класс `.bg-brand-gradient` sky→indigo, кольца фокуса, `prefers-reduced-motion`);
  инлайн-SVG-иконки `src/components/icons.tsx` (Lucide-стиль, без зависимостей) —
  в нижней навигации и ключевых кнопках вместо эмодзи; примитивы `Button` (варианты
  primary/secondary/ghost/danger, `loading`, active-scale) и `Card` (`interactive`).
  Переработаны: шапка/навигация, дашборд (стрик-герой на градиенте + цветные плитки),
  хаб «Практика», экран входа, placement. Остальные экраны наследуют примитивы.
  Скилл `ui-ux-pro-max` (в `~/.claude/skills/`) — гайд по дизайну (CLI-база симлинков битая,
  но текст SKILL.md рабочий).

## Язык общения
Отвечай пользователю **по-русски**. Он новичок в программировании, готов
копипастить и следовать пошаговым инструкциям. Claude работает напрямую в его
VS Code (есть доступ к файлам и терминалу) — поэтому фичи собираются прямо в
проекте и тут же тестируются, а не отдаются «воркерам» вручную.

## Стек
- Frontend: **Vite + React 19 + TypeScript + Tailwind v4** (`@tailwindcss/vite`), PWA (`vite-plugin-pwa`)
- Бэкенд/данные/вход: **Supabase** (Postgres + Auth + RLS). URL: `https://qyvkyyjqqqirmdliddsn.supabase.co`
- AI: **Gemini Flash** (free tier) через **Vercel serverless** `/api/gemini` (ключ прячется на сервере)
- Озвучка/распознавание: **Web Speech API** (браузер, бесплатно; en-US и es-ES)
- Словарь EN: **Free Dictionary API** (`dictionaryapi.dev`, без ключа)
- Словарь ES: локальные паки слов → fallback на Gemini (`lib/spanishDict.ts`)
- Интервальное повторение: **ts-fsrs** (алгоритм FSRS)
- Хостинг: **Vercel**

## Источники правды
- Архитектура и контракты: `docs/ARCHITECTURE.md` (названия папок, типы, схема БД, сигнатуры — не отклоняться)
- Схема базы: `docs/schema.sql` (уже выполнена в Supabase)

## Структура
```
src/
  lib/        supabase.ts, cards.ts (addCard, getDefaultDeck(lang), addCardsBulk),
              fsrs.ts (getDueCards(limit, lang)), dictionary.ts (EN),
              spanishDict.ts (ES: паки → Gemini), speech.ts (en-US/es-ES),
              gemini.ts (клиент /api/gemini), activity.ts (стрик)
  types/      index.ts — все общие типы (+ AppLang, Spanish*)
  context/    AuthContext.tsx, LanguageContext.tsx (EN/ES)
  data/       spanish/ — контент из приложения spanish (JSON + index.ts)
  components/ Button, Card, Layout (шапка с EN/ES), BottomNav, ProtectedRoute
  features/   dashboard, flashcards (+PacksSheet — паки исп. слов),
              reader (+SpanishReader — исп. тексты и диалоги), pronunciation,
              conversation (внутри — режимы Чат и Письмо, промпты EN/ES)
api/          gemini.ts (Vercel serverless), _core.ts (общий вызов Gemini)
vercel.json   SPA-rewrite для деплоя (не перекрывает /api/*)
```
Локальный dev-эндпоинт /api/gemini живёт в vite.config.ts (Vercel CLI не нужен);
ключ — строка `GEMINI_API_KEY=...` в `.env.local` (БЕЗ префикса VITE_).
Нижняя навигация: Главная / Колода / Ввод / Речь / Диалог.

## Как запустить
```
npm install      # если нужно
npm run dev      # http://localhost:5173
npm run build    # проверка типов (tsc -b) + сборка
```
В `.env.local` уже прописаны `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`.

## Статус (на момент переноса проекта в D:\projects\recall-app)
Готово и протестировано:
- ✅ Фаза 0 (аккаунты, Node), Фаза 1 (Фундамент: вход, навигация, Supabase, типы)
- ✅ Фича 1 «Колода» — FSRS-повторение + ручное добавление слов (`features/flashcards`, `lib/fsrs.ts`)
- ✅ Фича 2 «Ввод» — чтение текстов, тап по слову → словарь → в колоду (`features/reader`, `lib/dictionary.ts`, `sampleTexts.ts`)
- ✅ Фича 3 «Речь» — шэдоуинг: озвучка + распознавание + оценка в % (`features/pronunciation`, `lib/speech.ts`). Только Chrome/Edge, нужен микрофон.

Собрано, ждёт ручного теста пользователя (2026-07-02):
- 🔶 **Фича 4 «AI»** — Gemini-прокси + чат и проверка письма во вкладке «Диалог». Живой вызов Gemini через dev-эндпоинт проверен (ответ с исправлением ошибки пришёл).
- 🔶 **Фаза 3, ядро** — `lib/activity.ts` (стрик, «сделано сегодня», день в местном времени), запись активности из всех 4 фич, живой дашборд (стрик, счётчик карточек к повторению, бейджи «✓ сделано»).
- 🔶 **Иконки PWA** — сгенерированы в `public/` (192/512/maskable + favicon.svg + apple-touch-icon), прописаны в манифесте; установка приложения теперь возможна.

Собрано, ждёт ручного теста пользователя (2026-07-07):
- 🔶 **Мультиязычность EN/ES** — объединение с приложением spanish. Переключатель
  в шапке; у колод появился `lang`; все 4 фичи работают на обоих языках; паки
  испанских слов A1/A2 («📦 Паки» в Колоде), исп. тексты и диалоги в «Вводе»,
  исп. фразы в «Речи», исп. промпты в «Диалоге».
  Схема с `decks.lang` уже выполнена пользователем в Supabase (2026-07-07). ✔

- ✅ **Деплой на Vercel (2026-07-07)** — https://recall-pgkz.vercel.app, репозиторий
  https://github.com/kinjisei/recall (private). Автодеплой: пуш в `main` → Vercel сам
  пересобирает. Env в Vercel: GEMINI_API_KEY, VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY.
  В ESM-импортах внутри `api/` обязательно расширение `.js` (иначе
  FUNCTION_INVOCATION_FAILED). Прод проверен E2E-прогоном (EN и ES сценарии).

Собрано, ждёт ручного теста пользователя (2026-07-07, вторая партия):
- 🔶 **Весь испанский словарь** — все паки A1–B2 (~4668 слов, ~281 тема, включая
  частотный CSV) в `src/data/spanish/words/`; ленивый `words.ts` (отдельный чанк,
  не в стартовом бандле — он даже похудел до 725 КБ). «Паки» в Колоде: группировка
  по уровням, сворачивание секций, поиск. Reader-словарь теперь ищет по всем словам.
- 🔶 **Грамматика (испанский)** — экран `features/grammar/GrammarPage.tsx`, два раздела:
  «📖 Уроки» (74 урока A1–B2, ленивый `grammar.ts` ~127 КБ; теория + упражнения
  mcq/fill/order) и «🔀 Глаголы» (`ConjugationSection.tsx` + ленивый `conjugation.ts`
  ~64 КБ: справочник 14 времён с таблицами + тренажёр 110 упражнений на форму глагола).
  Вкладка «📚 Грам.» и карточка на дашборде — только в ES; итог → стрик. Базу НЕ меняет.
- 🔶 **Практика (испанский)** — `features/practice/`: хаб + 4 мини-игры (Подбери пару,
  Перевод, Собери предложение, Аудирование) на перенесённых словах/фразах. Карточка
  «🎮 Практика» на дашборде (только ES); завершение раунда → стрик. Базу НЕ меняет.
- 🔶 **Placement-тест** — `features/onboarding/PlacementTest.tsx` (роут `/placement`).
  48 вопросов A1–B2 (по 5 на уровень), результат → уровень испанского в localStorage
  (`lib/esLevel.ts`), подстраивает «Диалог». Карточка на дашборде (ES, пока не задан).
- 🔶 **Дизайн-система (полировка всего проекта)** — токены/базовые стили в `index.css`,
  инлайн-SVG-иконки `components/icons.tsx` вместо эмодзи в навигации, обновлённые
  `Button`/`Card`, переработаны шапка/навигация, дашборд (градиентный стрик-герой +
  цветные плитки), хаб «Практика», Login, placement. Светлая/тёмная тема, focus-кольца,
  safe-area, `prefers-reduced-motion`. Собрано по гайду скилла `ui-ux-pro-max`.
  Редизайн проверен смоуком в headless Edge после слияния (логин, дашборды EN/ES,
  грамматика, практика, placement, экран преподавателя) — JS-ошибок нет.
- ✅ **Фаза 4 «Преподаватель» (2026-07-07, слита в main)** — `features/teacher/`,
  `lib/teacher.ts`, блок «ФАЗА 4» в schema.sql (УЖЕ выполнен пользователем в Supabase).
  Код-приглашение → привязка ученицы → назначение колод → карточки в её очереди
  (RLS, расписание FSRS у каждой своё) → прогресс у преподавателя. Полный E2E пройден.
  Роль преподавателя выдаётся SQL-ом: `update public.profiles set role='teacher'
  where id = (select id from auth.users where email='...');`

- 🔶 **Контент-пак №1 (2026-07-11, собрано и проверено смоуком)** — выравнивание
  английского и добавка испанского:
  • EN паки слов: `src/data/english/words/` (B1/B2/C1, 18 тем, 342 слова, авторские)
    + ленивый `words.ts`; «📦 Паки» в Колоде теперь и для EN (PacksSheet принимает lang);
  • EN «Ввод»: +14 текстов (уровень B2 добавлен в переключатель), см. `sampleTexts.ts`;
  • EN «Речь»: 60 встроенных фраз B1–C1 с переводом (`src/data/english/sentences.json`),
    фразы из карточек добавляются после встроенных;
  • ES: +6 текстов (ids 41–46), +6 диалогов (ids 26–31), +30 фраз (ids 106–135).
  Типы: EnglishWord, WordTopic (алиас SpanishTopic) в `src/types`.

- 🔶 **Английская грамматика, Этап 1 из 5 (2026-07-11)** — вкладка «Грам.» теперь
  и в EN-режиме (в EN только «Уроки», «Глаголы»-спряжения остаются ES-only).
  Уроки: `src/data/english/grammar/` + ленивый `grammar.ts`; формат тот же, что у
  испанских (theory: paragraph/table/example с полем `en`; exercises: mcq/fill/order).
  Готовы уровни: **A1 — 12 уроков** (to be, местоимения, артикли, мн. число,
  Present Simple ±?, this/that, there is/are, have got, can, предлоги) и
  **A2 — 14 уроков** (was/were, Past Simple прав./неправ./вопросы, Present
  Continuous и vs Simple, going to, will, сравнения, превосходная, some/any/
  much/many, must/have to, me/him/her, like doing/want to do).
  **B1 — 14 уроков** (Present Perfect ×3: формы/спутники/vs Past Simple,
  for/since, Past Continuous, условные 0-1-2, пассив, should/could/might,
  used to, who/which/that, герундий vs инфинитив, косвенная речь, question tags).
  **B2 — 12 уроков (2026-07-20)** (Present Perfect Continuous, Past Perfect,
  Future Continuous/Perfect, Third Conditional, wish/if only, модальные+have+V3,
  пассив во всех временах, have sth done, косвенные вопросы/просьбы, relative
  clauses с запятыми, although/despite/however, remember doing vs to do).
  **C1 — 8 уроков (2026-07-20)** (инверсия Never have I…, условные без if +
  mixed, cleft-предложения, причастные обороты, нереальное прошлое it's time,
  would/will привычки, needn't have vs didn't need to, however hard/no matter).
  Стандарт качества: простой русский, таблица форм, озвучиваемые примеры,
  блок «⚠️ типичная ошибка русскоговорящего», 8–9 упражнений.
  **ВСЕ 5 ЭТАПОВ ГОТОВЫ: 60 уроков A1–C1 (пробелы — docs/grammar-gap-report.md), 180 упражнений в B2/C1.**
  **Тренажёр неправильных глаголов (2026-07-20)** — вкладка «Глаголы» теперь
  и в EN: `features/grammar/IrregularVerbsSection.tsx` + ленивые данные
  `src/data/english/irregular.ts` — **147 глаголов** (практически все ходовые)
  в 5 группах по типу изменения: не меняются (22), V2=V3 (71), V1=V3 (4),
  i-a-u (9), три разные формы (41). «Справочник» (таблицы с озвучкой + поиск
  по любой форме и переводу) и «Тренажёр» (раунд из 10, печатаешь V2 и V3,
  чипы выбора группы или «Все»; варианты через «/» принимаются — was/were,
  burnt/burned, got/gotten; итог → стрик, блок «Повтори» с ошибками).
  GrammarPage: «Глаголы» для обоих языков (ES — спряжения, EN — неправильные).

- 🔶 **Материалы преподавателя, фаза A (2026-07-19)** — генератор текстов с
  упражнениями: `lib/materials.ts` (двухшаговая генерация через /api/gemini:
  план → материал, ответы строго JSON с валидацией), вкладка «📝 Материалы»
  на экране преподавателя (форма: язык/уровень/тема/формат/длина/слова/
  грамматика → план от AI с правками → предпросмотр с ответами → сохранить →
  назначить ученицам), у ученицы карточка «Задания» на Главной + `/assignments`
  (текст → упражнения → сдача, авто-балл, статус submitted). Движок упражнений
  вынесен в `components/exercises.tsx` (грамматика переиспользует его же).
  Схема (блок «МАТЕРИАЛЫ ПРЕПОДАВАТЕЛЯ») УЖЕ выполнена пользователем в Supabase
  (2026-07-19); полный E2E пройден: генерация → сохранение → назначение →
  прохождение ученицей 13/13 → балл у учителя; RLS-проба (чужой не видит) ок.
- 🔶 **Материалы, фаза B — проверка работ (2026-07-19, E2E пройден)** —
  сданная работа: AI делает первичный разбор (generateAiReview, кэшируется в
  ai_review; ok может отличаться от авто — опечатки/синонимы), учитель на
  экране проверки (`ReviewScreen.tsx`) по каждому упражнению соглашается или
  ставит свой вердикт ✓/✗ + комментарий → «Завершить проверку» (teacher_review,
  статус reviewed). Уведомления: «Работ на проверку: N» на Главной учителя и
  бейдж на вкладке «Материалы». Ученица видит разбор: балл учителя и
  комментарии по каждому упражнению (`ReviewedView` в AssignmentsPage).
- ✅ **Материалы, фаза C — печать/PDF (2026-07-19)** — в карточке материала
  кнопки «🖨 Для ученика» (без ответов, с полями Name/Date и линейками для
  ответов) и «🖨 С ответами» (+ ключ для учителя). `PrintSheet.tsx` рендерится
  порталом в body; @media print в index.css прячет #root — печатается только
  лист. «Сохранить в PDF» — штатный диалог печати браузера (и на iPhone).
  Проверено генерацией настоящего PDF (page.pdf с print-стилями).

- 🔶 **Контекстный словарь + правки читалок (2026-07-19)** — общая шторка
  слова  для Ввода EN/ES и Заданий: перевод слова
  В КОНТЕКСТЕ предложения через Gemini (, кэш; will →
  «будет (вспом. глагол)», is → be «является (связка)»); Free Dictionary
  остался только для транскрипции/аудио EN. Шторка поверх навигации (z-50),
  прокрутка, кнопки всегда видны. Токенизация: длинные тире/многоточия —
  разделители («exposure—reading» больше не одно слово). В карточку слова
  пишется база, контекстный перевод и предложение-пример.
  **Две модели Gemini**: тяжёлое (генерация материалов, чат, письмо) —
  gemini-2.5-flash; лёгкое массовое (контекстный словарь, AI-разбор работ) —
  gemini-3.1-flash-lite (opts.light в chat(); ОТДЕЛЬНАЯ дневная квота —
  выяснено, когда за день тестов сожгли квоту flash). Белый список моделей
  в api/_core (ALLOWED_MODELS). gemini-2.5-flash-lite для новых ключей
  недоступна (404).

- 🔶 **Свайп-колода + перепроверка слов (2026-07-19)** — «Колода» переделана:
  карточка почти на весь экран (SwipeCard.tsx), тап — переворот (перевод+пример),
  свайп вправо=good, влево=again (два грейда вместо четырёх, «Легко» убрано);
  again-карточка возвращается в конец текущей очереди (reviewCard теперь
  возвращает свежий ReviewState); запасные кнопки ✗/✓; обучающий оверлей для
  новичка (localStorage recall.deck_tutorial_seen). Перепроверка слов:
  lib/wordChecks.ts + таблица word_checks (учитель видит слова учениц через
  новые RLS-политики чтения decks/cards/review_states привязанных учениц);
  у учителя в карточке ученицы раздел «Слова и перепроверка» (статусы
  новое/учится/изучено по интервалу FSRS, галочки → назначить), у ученицы
  плашка в Колоде → печатание слова по переводу (WordCheckRunner), неверные →
  again; результат с деталями у учителя. ⚠️ Блок «ПЕРЕПРОВЕРКА СЛОВ» из
  schema.sql должен быть выполнен в Supabase. Свайпы прогнаны E2E; перепроверка
  ждёт SQL для E2E.

- 🔶 **Ревью безопасности + оптимизация (2026-07-20)** — по двум ревью:
  • RLS-дыры (подделка баллов/вердикта/роли через прямой REST): все чувствительные
    записи → security-definer RPC (submit_material с СЕРВЕРНЫМ пересчётом балла,
    finish/reassign/assign_material, submit/assign_word_check, ensure_invite_code);
    прямая запись запрещена (revoke insert/update/delete на material_assignments,
    word_checks; revoke update(role)/insert/delete на profiles). USING-фиксы:
    экс-преподаватель после отвязки теряет доступ. review_states with check —
    только доступная карточка.
  • /api/gemini: авторизация (Supabase JWT) + CORS-белый-список + лимиты входа
    (был открытый прокси — любой мог жечь квоту). Клиент шлёт session token.
  • Бандл 900→287 КБ (ленивые роуты). ErrorBoundary + navigateFallbackDenylist
    /api/ (белый экран после деплоя на старом чанке → авто-reload). a11y: убран
    maximum-scale. ARCHITECTURE.md синхронизирован.
  • Баги логики: потеря карточки при ошибке свайпа, двойной свайп, задвоение
    балла заданий, двойной штраф FSRS, гонки WordSheet/AI-разбора — исправлены.
  ⚠️ Осознанный остаток: answer упражнений уходит на клиент (проверка клиентская),
    но балл серверный — «подглядывание» не даёт балл. npm audit: 10 в dev-цепочке
    @vercel/node (не в прод-бандле).
  ⚠️ Порядок деплоя: сначала выполнить schema.sql в Supabase, ПОТОМ push
    (клиент зовёт RPC, которых иначе нет). E2E teacher-фич требуют service_role.

- ✅ **Ревью запушено в прод (2026-07-20)** — schema.sql выполнен пользователем,
  защита проверена скриптом test-rpc-security.mjs: 13/13 (пробы подделки балла/
  вердикта/роли/результатов отклонены, серверный пересчёт работает). Заодно
  пойман и исправлен баг submit_word_check (row_count int, не boolean).
  В .env.local есть SUPABASE_SERVICE_KEY (gitignored) — для E2E teacher-фич.

- ✅ **PWA-обновления + анимации (2026-07-20)** — main.tsx: registerSW +
  reg.update() на visibilitychange и раз в час (iOS проверяет новую версию
  только при холодном старте — из-за этого на телефонах неделями жил старый код;
  «кнопка за навигацией» в шторке слова была именно этим). BottomNav: плавающий
  градиентный кружок-индикатор скользит к активной вкладке, иконка приподнимается.
  Button: ripple-волна от точки нажатия (все варианты). Шторка слова проверена
  визуально на 390×844 (iPhone 12): кнопка видна, кликабельна, слово добавляется.

- 🔶 **Хаб «Слова» (2026-07-20)** — «Колода» превращена в раздел изучения слов
  для обоих языков; испанская «Практика» влита туда же (вкладка убрана, роут
  `/practice` редиректит на `/flashcards`, папка `features/practice` удалена).
  `features/words/`: WordsPage (плитка «Повторение» с числом карточек к разбору
  + сетка мини-игр), MatchMode, QuizModes (Пропуск/Перевод/Аудирование на общем
  QuizRunner из GameShell), SentenceBuilder (ES), gameUtils. Прежняя колода —
  `features/flashcards/DeckReview.tsx` (режим внутри хаба).
  • **Match «слово ↔ значение»** — в EN правый столбец это АНГЛИЙСКОЕ определение
    (`lib/definitions.ts`: кэш localStorage → Free Dictionary → Gemini light),
    в ES — перевод. Определение выбирается не первое, а самое учебное: часть речи
    подсказывает русский перевод карточки («храбрый» → adjective, «шептать» → verb),
    само слово в определении маскируется, толкования через само себя отсеиваются.
    Известное ограничение: у многозначных слов иногда всплывает устаревшее значение
    (harvest → «the third season of the year»).
  • **Материал игр** — `lib/wordPool.ts`: СНАЧАЛА карточки пользователя (с их
    ReviewState), паками добираем только нехватку; `pickWords()` держит приоритет.
  • **Связь с FSRS** — ошибка в игре по слову из колоды = `reviewCard(..., 'again')`
    (карточка вернётся на повтор), верный ответ расписание НЕ трогает.
  E2E: все режимы EN, редирект /practice, ES «Собери фразу»; отдельный тест FSRS
  (ошибка → relearning+lapses, остальные 7 карточек нетронуты) и качества определений.

- 🔶 **Редизайн «Nocturne» (2026-07-20)** — весь интерфейс переведён на тёмную
  тему по макету из `handoff/` (ТЗ: `handoff/handoff/CLAUDE-CODE-TASK.md`).
  • **Дизайн-система**: токены `--night-*` в `index.css` (фон #161826, акцент
    #9184d9, поверхности #1e2030), анимации (fade-up, аврора, flame, pop-in,
    bar-grow, pulse-ring, confetti), `.lift`. Шрифт **Onest** локально
    (`@fontsource-variable/onest`) — PWA работает офлайн. Тема ЕДИНАЯ тёмная:
    `class="dark"` на `<html>` + `@custom-variant dark` — старые dark:-классы
    срабатывают всегда. Иконки **Phosphor**.
  • **Вход**: аврора-фон, glass-панель формы на мобильных, hero на десктопе.
  • **Каркас**: 5 постоянных вкладок (Главная · Слова · Учёба · Речь · Диалог)
    в плавающей glass-капсуле; TopBar с EN/ES и аватаром → /progress;
    новый хаб **«Учёба»** (`/study`): тексты, грамматика, placement.
    Общий компонент `RowCard` — карточка-строка (база всех списков).
  • **Главная**: стрик-герой (градиент, пламя, 7 полосок недели), «Начать
    занятие» — ведомая сессия слова→чтение→речь (`lib/guided.ts` +
    `GuidedNext`, шаг в sessionStorage), план на сегодня, слово дня.
  • **Онбординг** `/onboarding` (3 шага) — только для новичка (нет активности
    и нет флага `recall.onboarded`); уровень EN → в профиль, ES → placement.
  • **Прогресс** `/progress`: график недели + метрики (слов изучено, точность
    как доля повторений без срыва, лучшая серия, к завтрашнему дню), выход.
  • **Празднование**: `celebrate()` — вибрация + конфетти (портал, ~2.2с).
  • **Этап 7**: Card/Button на токенах, сквозная перекраска 26 файлов;
    колода — 3D-разворот и кнопки «Ещё раз»/«Помню»; речь — пульсирующий
    микрофон; диалог — accent-пузыри. Печать (PrintSheet) осталась белой.
  ⚠️ Решения по нестыковкам ТЗ (согласованы): вкладка «Колода» = наш хаб
    «Слова» (мини-игры туда уже влиты, отдельной «Практики» нет); шрифт Onest
    (в тексте ТЗ был Inter, в CSS — Onest); тема только тёмная.
  Бандл 352 КБ (gzip 111). Обход 11 экранов автопоиском светлых пятен — чисто.

- ✅ **«Мои слова» (2026-07-20)** — раздел «Слова» получил список собственных
  карточек: `features/words/MyWords.tsx` + `lib/cards.ts` (listMyWords,
  updateCard, deleteCard, countMyWords). Поиск по слову/переводу/примеру,
  фильтр по статусу (новое/учу/выучено — общий `statusOf` из wordChecks),
  правка прямо в строке, удаление с подтверждением, озвучка. Схему БД менять
  НЕ нужно: политика «cards via own deck» разрешает владельцу update/delete,
  review_states уходит каскадом. E2E пройден (правка опечатки и удаление
  проверены в базе; удалённое слово не приходит на повторение).

- ✅ **Чистка структуры (2026-07-20)**:
  • «Учёба» и «Ввод» слиты: вкладка `/study` сразу показывает список текстов,
    грамматика и placement — строками над ним (`ReaderPage` получил слот
    `header`, видимый только в списке, и настраиваемый `title`). Чтение было
    в двух тапах — стало в один. `/reader` редиректит на `/study`; план дня
    и `lib/guided.ts` ведут туда же.
  • Аватар в шапке → меню (`AvatarMenu` в Layout): «Мой прогресс», «Мои
    ученицы» (только role=teacher), «Выйти». Раньше вход к преподавателю был
    лишь карточкой внизу Главной.
  • «Паки» и «+ Слово» перенесены из режима «Повторение» в шапку хаба
    «Слова»; `AddCardForm` вынесен в `features/words/AddCardForm.tsx`.
  E2E: 10 проверок (тексты в один тап, редирект, кнопки в хабе и их
  отсутствие в повторении, меню ученика без «учениц» и преподавателя с ними).

- ✅ **Настройки и задания в «Учёбе» (2026-07-20)**:
  • `/settings` (`features/settings/SettingsPage.tsx`, пункт в меню аватара):
    имя и уровень языка → в профиль (RLS разрешает только эти колонки; уровень
    ES остаётся локальным), скорость озвучки (`lib/settings.ts` → `speak()`
    берёт rate, если он не задан явно) и размер текста в чтении (применяется
    в EN- и ES-читалках). Локальные настройки — в localStorage, у каждого
    устройства свои.
  • «Задания от преподавателя» — строка в «Учёбе» со счётчиком новых. Раньше
    задание можно было открыть только карточкой на Главной, и только пока оно
    новое; выполненные становились недоступны.
  ⚠️ Напоминаний (push) нет намеренно: в PWA на iOS требуют сервера и
    разрешений — кнопку-пустышку не делал.

- ✅ **Аудит доступности (2026-07-20)** — по правилам скилла `ui-ux-pro-max`
  (§1 Accessibility, §2 Touch). Скрипт обошёл 8 экранов и ЗАМЕРИЛ реальные
  значения: было 155 замечаний, стало 1 (ссылка внутри предложения —
  допустимое исключение WCAG 2.5.5 для inline-целей).
  • Контраст: токены вторичного текста давали 3.57:1 и 2.08:1 при норме 4.5.
    Пересчитаны под фон #161826: `--night-text-70` 0.75 (~8.4:1), `-60` 0.66
    (~6.8:1), `-40` 0.58 (~5.3:1). Самый бледный `-25` (0.4, ~3.3:1) —
    ТОЛЬКО для иконок и разделителей (норма 3:1), в тексте не использовать.
  • Тач-цели ≥44×44: пароль, аватар-меню, EN/ES, логотип, «Назад», уровни,
    вкладки «Чат/Письмо», фильтры «Моих слов», чипы настроек, «Прослушать».
  • Подписи: input↔label через id/htmlFor, полю чата aria-label.
  ⚠️ CLI скилла недоступен (симлинки `scripts`/`data` ведут в несуществующую
    `../../../src/ui-ux-pro-max`) — работал по тексту SKILL.md. Сам скилл
    написан под React Native, у нас веб-PWA: брались универсальные правила.
  Скрипт аудита переиспользуемый — стоит гонять после правок вёрстки.
  (2026-07-21 скрипт переписан и СОХРАНЁН в репо: `scripts/ux-audit.mjs` —
  `node scripts/ux-audit.mjs` при работающем dev-сервере; сам создаёт и удаляет
  тестовый аккаунт через service_role, отчёт → ux-audit-report.md, gitignored.
  ⚠️ puppeteer.launch с Edge не работает при открытом пользовательском Edge —
  скрипт запускает msedge сам и цепляется через puppeteer.connect.)

- ✅ **Повторный UX-аудит (2026-07-21)** — прогон по 10 экранам (вход, главная,
  слова, учёба, грамматика, речь, диалог, прогресс, настройки, placement):
  было 8 замечаний → 0. Исправлено: тач-цели <44px (аккордеоны уровней в
  грамматике, «На главную» в placement — min-h-11; переключатель «Войти» на
  логине — расширение хит-зоны `-m-3 p-3` без сдвига вёрстки) и кнопка
  отправки в «Диалоге»: глиф ➤ → Phosphor PaperPlaneRightIcon + aria-label.

- 🔶 **Доводка Nocturne + мини-игры, этапы A–E (2026-07-21, собрано и прогнано
  смоуком; ждёт ручного теста)** — по остаткам ТЗ `handoff/CLAUDE-CODE-TASK.md`:
  • **A. Полировка Учёбы/Речи/Диалога**: эмодзи в заголовках/вкладках →
    Phosphor-иконки (💬🎙📄🎭🔊🇷🇺 и др.), инпуты чата/письма/упражнений на токене
    `--night-input`, в «Диалоге» строки AI «✏️ …»/«✅ …»/«📚 …» подсвечиваются
    акцентом/emerald (AssistantText), sky/slate-hover в упражнениях и
    PlacementTest переведены на токены, тач-цели вкладок читалки ≥44px.
  • **B. Колода до ТЗ** (SwipeCard/DeckReview): настоящий 3D-переворот с
    задней гранью (rotateY 180°, backface-visibility, 650мс пружина), штампы
    «Помню» (accent) / «Ещё раз» (нейтральный), вибрация на свайп, прогресс-бар
    раунда, финал — SealCheck + конфетти (celebrate()).
  • **C. Единая проверка ответов**: `lib/text.ts answerMatches` — варианты
    через «/» («was/were») теперь принимаются и в fill-упражнениях; order
    сравнивает слова нормализованно; тренажёр неправильных глаголов
    переиспользует ту же функцию. ⚠️ `submit_material` в schema.sql обновлён
    под те же правила (fill-варианты + оценка order, которая раньше ВООБЩЕ не
    давала балл) — **перед деплоем выполнить блок в Supabase!**
    Новый `scripts/validate-exercises.mjs` — структурный валидатор всех данных
    (1408 записей: уроки EN/ES, окончания, глаголы, оба placement) — чисто.
  • **D. Placement 2.0**: EN-тест с нуля — `src/data/english/placement_test.json`
    (60 авторских вопросов A1–C1) + ленивый `placement.ts`; ES-пул расширен
    48→60 (по 15 на уровень A1–B2). Тест языкозависимый: по 10 случайных на
    уровень (EN до 50, ES до 40), блоками от простого к сложному, ранний стоп
    при <40% в блоке. Результат: ES → localStorage, EN → profiles.level.
    Строка «Определи свой уровень» в «Учёбе» теперь и для EN (пока в профиле
    нет уровня).
  • **E. Мини-игры в «Словах»**: новые «Спринт» (`SprintMode.tsx`, 60 секунд
    верно/неверно, вибрация, ошибка → FSRS again) и «Диктант»
    (`DictationMode.tsx`, озвучка → печать слова, проверка answerMatches);
    «Собери фразу» теперь и для EN (на 60 встроенных фразах). В грамматике —
    банк **«Мои ошибки»** (`lib/mistakes.ts`, localStorage): неверный ответ в
    уроке кладёт упражнение в банк, верный (в уроке или повторе) — убирает;
    строка с счётчиком над списком уроков → экран повтора.
  Смоук: `scripts/ux-audit.mjs` расширен ES-экранами (15 экранов, 0 замечаний,
  0 JS-ошибок); новый интерактивный `scripts/smoke-features.mjs` (10/10:
  спринт/диктант/фраза EN/placement EN с ранним стопом/«Мои ошибки»/диалог).
  Оба скрипта сами создают и удаляют тестовый аккаунт через service_role.
  Этапы A–E запушены в прод (98afbea), submit_material выполнен в Supabase. ✔

- 🔶 **Этап F: новая навигация + хаб «Практика» + AI под уровень (2026-07-21,
  собрано, смоук 13/13 и аудит 15 экранов чисто; ждёт SQL + пуша)** — по
  замечаниям пользователя (новичок терялся в 5 вкладках) и прототипу
  `Recall Приложение.dc.html` (в корне, gitignored как *.dc.html):
  • **Навигация: 4 вкладки** — Главная · Учёба · Практика · Диалог
    (BottomNav.tsx). «Слова» и «Речь» больше не вкладки: смысловое деление —
    Учёба «изучаю новое», Практика «тренируюсь», Диалог «общаюсь».
  • **Хаб «Практика»** (`features/practice/PracticePage.tsx`, роут /practice;
    /flashcards → редирект) — развитие хаба «Слова» (WordsPage удалён):
    плитка «Повторение» + секции: СЛОВА (Мои слова, Значения, Пропуск,
    Перевод, Аудирование, Спринт, Диктант, Собери фразу — компоненты остались
    в features/words/), ГРАММАТИКА (новый «Микс упражнений» —
    `features/practice/GrammarMixMode.tsx`, 8 случайных упражнений из уроков
    не выше уровня, ошибки → банк «Мои ошибки»; «Мои ошибки» →
    /grammar?mistakes=1; «Глаголы» → /grammar?verbs=1 — GrammarPage понимает
    эти query-параметры), РЕЧЬ (→ /pronunciation). Guided-шаг flashcards
    ведёт на /practice.
  • **Учёба**: строка «Практика» (как в прототипе) + тест уровня доступен
    ВСЕГДА: если уровень известен — «Твой уровень: X · пройти заново»
    (преподаватель может просто попросить пройти заново; отдельного
    механизма назначения теста в БД нет — сознательно).
  • **AI под уровень ученика** (жалоба: «стул» объяснялся энциклопедично):
    `lib/level.ts` (getUserLevel: ES — localStorage, EN — profiles.level);
    `lib/definitions.ts` — для A1/A2 Free Dictionary пропускается, Gemini
    пишет сверхпростые определения (кэш с префиксом "s:", уровни не
    смешиваются), MatchMode передаёт уровень; промпт чата — явная ветка
    A1/A2 (короткие фразы, частотные слова) + правило «объясняй как школьнику,
    без лингвожаргона»; промпт письма и контекстный словарь (contextDict) —
    «просто, без терминов»; ссылки в промптах на вкладку «Грам.» → «Учёба».
  ⚠️ Перед пушем выполнить в Supabase новый блок schema.sql «PLACEMENT 2.0»:
    profiles.level теперь разрешает 'A1' (иначе EN-тест с результатом A1
    молча не сохраняет уровень).
  Этап F запушен в прод (a0bc948), SQL «PLACEMENT 2.0» выполнен. ✔

- 🔶 **Этап G: сверка со standalone-макетом + правки UX (2026-07-21, собрано,
  смоук 18/18, аудит 15 экранов — 0; ждёт пуша)** — по второй партии замечаний
  пользователя и «Recall Приложение (standalone).html»:
  • **Логотип**: в шапке слева полный BrandLogo (слово на карточке) вместо
    знака+текста.
  • **BackButton** (`components/BackButton.tsx`, + BackHeader): единая
    компактная кнопка-каретка 44×44 с тонкой рамкой слева от заголовка (как в
    макете) — заменила разнобой «← Назад»/«← Главная» во ВСЕХ экранах
    (читалки, грамматика, спряжения, колода, игры, задания, проверка).
  • **Учёба-хаб** (StudyPage переписан): строки Задания · «Тексты и диалоги»
    (список текстов теперь ЗА тапом; guided-шаг чтения идёт на
    /study?view=reader) · Грамматика · «Слова» (внутренний экран: Паки слов,
    Своё слово, Мои слова, Повторение колоды) · Тест уровня. Строка
    «Практика» из Учёбы УБРАНА (чёткое разделение вкладок).
  • **Практика**: кнопки Паки/+Слово убраны (теперь в Учёба → Слова);
    грамматика разделена на 3 мини-игры по типам заданий — «Выбери форму»
    (mcq), «Впиши слово» (fill), «Собери предложение» (order) — все на
    GrammarMixMode с параметром kind, задания ≤ уровня ученика.
  • **Слово дня** на Главной: теперь НОВОЕ слово из пака уровня (которого нет
    в колоде), стабильное в течение дня (wordPool.newWordOfDay), с кнопками
    озвучки и «В колоду» (addCard) — раньше показывалась своя же due-карточка
    и добавлять было нечего.
  • **Диалог по макету**: сегмент Чат/Письмо капсулой справа от заголовка,
    поле ввода без рамки (--night-input, rounded-14) + квадратная
    accent-кнопка отправки 48×48.
  • **Задания**: подпись «тренироваться →» на выполненных карточках — режим
    «Пройти ещё раз (без пересдачи)» существовал, но его нельзя было найти;
    работает и для сданных (на проверке), и для проверенных.
  ⚠️ SQL не требуется. Прототип и zip в корне — gitignored (*.dc.html, *.zip);
    копия макета разобрана в scratchpad.
  Этап G запушен в прод (53deb37). ✔

- 🔶 **Этап H: AI-квесты по грамматике (2026-07-21, собрано; RPC-смоук 9/9,
  живой UI-тест с настоящим Gemini 5/5, аудит 16 экранов — 0; SQL выполнен
  пользователем)** — текстовые игры в стиле Talkpal:
  • **БД**: блок «AI-КВЕСТЫ» в schema.sql — таблица grammar_quests
    (teacher/student, lang, level, topic, scenario, target 3–50, progress,
    status, messages jsonb) + RLS (чтение обеим сторонам, экс-учитель теряет
    доступ) + security-definer RPC: assign_grammar_quest,
    delete_grammar_quest, quest_correct_answer (серверный инкремент с клампом,
    порог → completed), save_quest_messages (лимит 200 КБ).
  • **Учитель** (`features/teacher/QuestSection.tsx`, раскрывашка в карточке
    ученицы): форма (язык/уровень/тема с datalist/6 пресетов сценариев + свой/
    ползунок порога) + список квестов с прогрессом + просмотр переписки
    ученицы с AI + «Снять».
  • **Ученица** (`features/quests/QuestsPage.tsx`, роут /quests; строка
    «AI-квесты» с бейджем в Учёбе — прячется, пока квестов нет): чат-раннер,
    AI ведёт сценарий под уровень; первая строка каждого ответа AI — служебный
    вердикт (START/CORRECT/TRY_AGAIN), клиент его парсит и скрывает; CORRECT →
    RPC-инкремент + прогресс-бар + logActivity('grammar'); TRY_AGAIN → «✏️ …»
    по-русски просто; порог → конфетти + completed. Переписка сохраняется
    после каждого хода (возобновление с любого устройства).
  • `lib/quests.ts` — клиентское API; тип GrammarQuest в src/types.
  ⚠️ Осознанный компромисс (как с answer у упражнений): вердикт ставит AI на
    клиенте → прогресс теоретически накручивается прямым вызовом RPC, но вся
    переписка видна учителю в QuestSection — накрутка палится глазами.
  Смоук: `scripts/smoke-quests.mjs` (9/9: назначение/видимость/чужой не
  видит/ученица не назначает/прогресс 3→completed/оверфлоу отклонён/переписка/
  удаление; сам создаёт-удаляет учителя+ученицу+постороннего через
  service_role). Живой UI-прогон: Gemini начал сценарий по-английски, ответ в
  Past Simple получил CORRECT, прогресс 1/3, VERDICT скрыт, messages в БД.

Ранее намеченное:
- Полировка: усилить ✏️-исправления в испанском промпте (иногда пропускает ошибки).
- Из аудита осталось за кадром: prefers-reduced-motion (глобально уже есть),
  порядок фокуса при навигации, alt у декоративных SVG (пока их нет).

## Правила
- Не ломать контракты из `docs/ARCHITECTURE.md`. Импортировать типы из `src/types`.
- Каждая фича — в своей папке `features/*` + свой файл `lib/*`. Общие файлы менять аккуратно.
- После изменений проверять `npm run build`.

- ✅ **Монетизация v1 (2026-07-22, в проде, E2E на проде пройден)** — тарифная
  механика по решениям грилинга (см. docs/monetization-draft.md, раздел
  «РЕШЕНИЯ ЗАПУСКА»): планы free/premium(1490₸)/teacher_mini(3000₸,5 уч.)/
  teacher_start(5000₸,10)/teacher_pro(12000₸,30); триал 14 дней всем новым
  (trial_until); Free навсегда = статика без лимитов + 5 AI-действий/день
  (consume_ai_quota → RECALL_FREE_LIMIT, paywall-текст в api/_auth.ts);
  ученица активного/триального учителя = premium-доступ (has_premium_access).
  Экран /pricing (публичный, Kaspi +7 776 210 02 21, email в комментарии
  перевода), /admin (is_admin: поиск по email → admin_set_plan на N месяцев).
  Блок «ТАРИФЫ И FREE-ЛИМИТЫ» в schema.sql ВЫПОЛНЕН пользователем; владелец
  (k.yerbolat.2004) — is_admin, девушка (a.abduldina19) — teacher_pro на 100 лет.
  Смоук на проде: 5 lite-запросов прошли, 6-й → 429 с текстом про Premium.
  AI-роутинг по сложности: tier lite/standard/max (перевод слова → Groq-8b/
  Gemini-lite; чат/письмо/разбор → flash-цепочка; материалы учителя →
  gemini-2.5-pro → 3-pro) + цепочки фолбэков на 429/404 в api/_core.ts.

- 🔶 **Диагностическая карта ученицы, этап 1 из 2 (2026-07-22, собрано; ждёт
  SQL + смоука)** — по просьбе владельца («индивидуальная карта студента»):
  • **БД**: блок «ДИАГНОСТИКА» в schema.sql — таблица grammar_mistakes
    (user_id, lang, topic_id, ex, unique) + RLS (свои — полностью, учитель
    привязанной ученицы — чтение, отвязка отбирает).
  • **Синк ошибок**: lib/mistakes.ts — банк «Мои ошибки» остаётся в
    localStorage (источник правды для ученика), но add/remove тихо дублируются
    в БД; syncMistakesToDb(lang) — разовая заливка старого банка (флаг
    recall.mistakes_synced.{lang}), зовётся при открытии грамматики.
  • **Карта**: lib/diagnostics.ts (getStudentDiagnostics: слова FSRS +
    буксующие по lapses≥2, задания с финальным % (teacher_review приоритетнее
    авто) + разбивка по kind, ошибки грамматики по темам, квесты, активность
    за 14 дней) + features/teacher/DiagnosticsSection.tsx — раскрывашка
    «Диагностическая карта» в карточке ученицы (TeacherPage). Только чтение.
    Если таблицы нет — карта не падает, показывает подсказку про SQL.
  • Смоук: scripts/smoke-diagnostics.mjs (RLS: своя запись/чтение учителем/
    посторонний и подделка отклонены/отвязка). Этап 2 (программа обучения по
    неделям, AI tier max) — следующим заходом.

- 🔶 **Программа обучения по неделям, этап 2 из 2 (2026-07-22, собрано; ждёт
  SQL + смоука)** — AI-план для ученицы:
  • **БД**: блок «ПРОГРАММА ОБУЧЕНИЯ» в schema.sql — таблица study_plans
    (teacher/student, lang, level, goal, summary, start_day, weeks jsonb,
    status active/archived; частичный уникальный индекс — одна АКТИВНАЯ на
    пару+язык; чек размера weeks < 200КБ) + RLS (учитель — всё для СВОИХ
    учениц, отвязка отбирает; ученица — только чтение своих).
  • **Генерация**: lib/studyPlan.ts — generateStudyPlan (tier max): промпт =
    уровень + цель учителя + СВОДКА ДИАГНОСТИКИ (слабые темы, буксующие слова,
    баллы по категориям) + каталог встроенных уроков грамматики (id·уровень·
    название; AI обязан брать topicId только из каталога, выдуманные id
    вычищаются санитайзером); строгий JSON + sanitize (типы, длины, обрезка).
    saveStudyPlan архивирует прежнюю активную; currentWeekIndex по start_day.
  • **Учитель**: features/teacher/ProgramSection.tsx (раскрывашка «Программа
    обучения» в карточке ученицы): EN/ES → активная программа (недели,
    текущая подсвечена, «Составить новую»/«Снять») или форма (уровень/недель
    2-8/цель) → предпросмотр → правки текстом → «Пересоставить» → «Сохранить».
  • **Ученица**: /program (features/program/ProgramPage.tsx) — программы с
    подсветкой текущей недели; строка «Моя программа» в «Учёбе» с номером
    недели (прячется, если программ нет). Общий рендер — program/PlanView.tsx.
  • Смоук: scripts/smoke-program.mjs (8 проверок RLS + уникальный индекс).
  SQL обоих блоков выполнен пользователем; смоуки 7/7 (диагностика) и 9/9
  (программа); запушено в прод (e8a7f0b) вместе с фиксом айфон-зума.
  ⚠️ Осталось: живой UI-тест AI-генерации программы (жжёт квоту Pro —
  проверить одной генерацией на реальной ученице).

- 🔶 **Заходы 1-2-6 доводки (2026-07-23, собрано, ждёт пуш-пакета)** —
  • Заход 1: кэш профиля везде (уровень без задержки; placement/онбординг
    сбрасывают кэш), балл переназначенных работ из attempts (общий
    lib/assignmentScore + тест 8/8), материалы сгруппированы по уровням,
    анти-повтор слов во всех играх (wordPool: история 40 слов в localStorage +
    случайный порядок внутри уровня; Dictation тоже), тач-цели «Речи».
  • Заход 2: DeckWordsPicker — учитель видит слова наборов (поиск при >30) и
    назначает ВЫБОРКУ галочками (assignSelectedWords: колода-копия «выборка
    для <имя>»); смоук smoke-deck-picker.mjs 6/6.
  • Заход 6: карточка «Тебе назначили программу» на Главной (флаг
    recall.program_seen.<id>, гаснет на /program); блок «Динамика за месяц»
    в диагностике (lib/dynamics, чистый модуль + тест 7/7: активные дни /
    средний балл по попыткам / новые ошибки / слова, окна 30+30 дней);
    «🖨 Отчёт для родителей» (ReportSheet: печатный лист с динамикой,
    сильными сторонами, фокусом месяца и комментарием учителя; позитивный
    фрейминг; @media print теперь красит фон страницы белым — тёмная полоса
    в печати материалов тоже ушла). Смоук smoke-report-pdf.mjs 4/4 с
    генерацией настоящего PDF (проверен визуально).
  • Скиллы: 46 в ~/.claude/skills (26 доустановлено воркером), гайд —
    docs/skills-guide.md (+docx-версии трёх доков в D:\Downloads\sounds\read).

- 🔶 **Фразовые глаголы EN (Заход 3, 2026-07-23)** — «Грамматика → Глаголы»
  в EN теперь два раздела: «Неправильные» и «Фразовые» (EnglishVerbs в
  GrammarPage). Данные: src/data/english/phrasal/phrasal.json + ленивый
  phrasal.ts — 30 базовых глаголов, 284 фразы (phrase/ru/example/exampleRu/
  separable/level B1-B2). Собраны воркфлоу 61 агента (Haiku-генерация →
  Sonnet-проверка каждой партии → критик полноты нашёл 25 проблем: дубли
  pick up ×5, книжные give rise to и т.п. — все исправлены скриптом +
  3 частотных добавлены вручную: look up to, get rid of, get together).
  UI: PhrasalVerbsSection — справочник (аккордеоны по глаголу, поиск по
  фразе/переводу, метки «разделяемый»/уровень, озвучка фразы+примера) и
  тренажёр (раунд 10: перевод + «look ___» → частица из 4; обманки сперва
  из частиц ТОГО ЖЕ глагола; итог → стрик, «Раунд из ошибок»).
  validate-exercises.mjs проверяет phrasal.json (структура/частицы/дубли/
  separable-подозрения); UI-смоук scripts/smoke-phrasal.mjs 6/6.
  ⚠️ Сверка с EFE Grammar Guide не удалась: юнит phrasal verbs в книге —
  картинки-панели без текстового слоя (вытянулись только заголовки
  упражнений); компенсировано строгой Sonnet-проверкой + критиком.

- 🔶 **Идиомы из EFE + фразовые глаголы ×2 (Заход 4, 2026-07-23)** —
  • **Идиомы**: 911 шт. из книги EFE English Idioms в 20 темах «Идиомы: <категория>»
    (10 категорий × B1/B2), src/data/english/words/words_idioms.json, подключены
    в words.ts — видны в «Паках» и попадают в игры (короткие; длинные >28 симв.
    остаются только паками — isPlayable не менялся).
    ⚠️ Текстовый слой книги содержал только значения — сами идиомы напечатаны
    КАРТИНКАМИ. Решение: pymupdf отрендерил 247 страниц в PNG, 42 vision-агента
    (Haiku) прочитали их глазами, Sonnet перевёл/раскидал по категориям и
    уровням, отдельный проход нормализовал словарные формы («kept in touch» →
    «keep in touch»), финальная сверка 20 случайных идиом с исходными
    страницами — 20/20. Смоук scripts/smoke-idioms.mjs 5/5 (пак добавляется).
  • **Фразовые глаголы, партия 2**: +30 глаголов (grow, find, figure, end,
    wake, watch, calm, cheer, hang, check…) тем же конвейером Haiku→Sonnet→
    критик (нашёл 39 проблем — calm down ×6, книжные figure up/as; вычищено).
    Итого 60 глаголов / 476 фраз. Нижняя граница валидатора 2-12 фраз:
    у find/calm частотных фраз объективно мало, мусором не добивали.
  • pymupdf установлен (pip) — теперь умеем OCR через vision-агентов; путь
    для Захода 7 (сканы 1-6.pdf, Upper/Advanced) проверен.

- 🔶 **Методичка преподавателя (Заход 5, 2026-07-23)** — вкладка «Методичка»
  на экране «Преподаватель» (features/teacher/GuideSection.tsx, контент —
  src/data/teacher-guide.ts). 4 раздела аккордеонами: цикл ведения ученицы
  (6 шагов с привязкой к инструментам Recall), чтение диагностической карты
  (сигнал → реакция), 6 методик с исследованиями и применением в Recall
  (интервальные повторения/comprehensible input/коррекция ошибок/лексический
  подход/TPR/вывод в речь; ресёрч — 7 Sonnet-агентов с WebSearch, 2-4 источника
  на метод, ссылки видны в UI под блоками), типичные ошибки репетитора
  (усилены находками ресёрча: Edutopia, Pearson, WLR). БД не трогает.
  ux-audit чисто (2 известных inline-исключения на «Речи»), скриншот проверен.

- 🔶 **OCR сканов → gap-отчёт 2.0 (Заход 7, 2026-07-23)** — 12 учебников-сканов
  прочитаны зрением (pymupdf → PNG первых 16 стр → sonnet-агенты): 1-6.pdf
  оказались «4000 Essential English Words 1-6» (Paul Nation) — лексические
  курсы БЕЗ грамматики (для gap-отчёта нерелевантны, но идеальный донор слов
  и однотипных упражнений — план согласован с владельцем: берём слова +
  структуру упражнений, тексты не публикуем). Headway Intermediate/Upper/
  Advanced SB+WB прочитаны напрямую: 28 пробелов подтверждены вторым
  источником, 13 НОВЫХ добавлены «(уточнено по OCR)» — итого 53 пробела
  (12🔴/21🟡/20⚪), топ-пробелы B1: -ed/-ing прилагательные, be/get used to.
  docs/grammar-gap-report.md обновлён.

- 🔶 **Лендинг /teachers (2026-07-23)** — публичная страница для репетиторов
  (features/landing/TeachersPage.tsx, лениво). Решение: внутри приложения, а
  не отдельный сайт (бесплатно, один домен, регистрация в 1 клик; вынесем,
  когда будет свой домен). Структура: hero (боль→обещание→CTA «14 дней без
  карты») + signature-элемент «мини-отчёт родителям» белой карточкой → 3 шага
  → 6 инструментов → «между уроками» (самостоятельные занятия) → цены
  3000/5000/12000₸ с per-student сравнением (500₸ vs ~1000₸ у аналогов) →
  FAQ → финальный CTA. Цифры из monetization-draft; выдуманных отзывов НЕТ
  (появятся после пилота). ⚠️ CTA ведёт на /login — заработает полноценно
  после открытия регистрации (SQL-блок готов).

- 🔶 **Заход 8: полировка экранов (2026-07-23, собрано, ждёт пуш-пакета)** —
  • «Учёба»: все async-строки (задания/квесты/программа) грузятся Promise.all
    и появляются ОДНИМ пакетом со stagger (позиционный delay-счётчик), пока
    грузятся — скелетоны высоты RowCard; «Твой уровень» мгновенно из
    localStorage-кэша (lib/profile: getCachedEnLevel, пишется при каждом
    fetchProfile, чистится при signOut — clearProfileCaches в AuthContext).
  • Мессенджер-паттерн для чатов (lib/useChatList): лента скроллится ВНУТРИ
    себя, шапка и заголовок всегда видны, клавиатура сжимает список, автоскролл
    к последнему сообщению. Применён в «Диалоге» и чате AI-квестов; у квестов
    панель ввода теперь фиксирована у низа (как в Диалоге).
  • lib/useScrollTop: сброс прокрутки при смене ВНУТРЕННИХ экранов —
    ReaderPage (открытие текста), SpanishReader, AssignmentsPage (active,
    stage), StudyPage (view), PracticePage (?m=), MaterialsSection (mode),
    QuestsPage (вход в чат).
  Проверено скриншотами (Учёба с заданием+квестом со stagger; чат квеста с
  живым AI-вступлением) и ux-audit (2 известных inline-исключения «Речи»).

- 🔶 **Заход 9: умные обманки + Спринт-антиповтор (2026-07-23, собрано)** —
  • lib/distractors.ts (чистый): pickDistractors — обманки ранжируются по
    похожести на правильный ответ (та же тема +4, часть речи по русскому
    переводу +3 (ruPos: -ть/-чь=глагол, -ый/-ая=прил., -о=наречие), уровень
    +2, близость длины; правильный ответ не выделяется длиной — крайняя
    обманка заменяется ближней). Подключено в optionsFor (Перевод, Пропуск)
    и в ложные пары Спринта; Аудирование оставлено на своей звуковой
    похожести (для аудио она правильнее); Match — пары, обманок нет.
  • lib/recentWords.ts (чистый): история показов вынесена из wordPool;
    Спринт подключён (withoutRecent при загрузке + recordShown на каждый
    ответ) — раньше был сознательно пропущен.
  • scripts/test-distractors.mjs 10/10 («шептать» → кричать/бормотать/
    разговаривать, «крокодил» отсеян; раунды не пересекаются; малый пул
    не пустеет). Сборка чистая.

- 🔶 **Заход 10: план дня от учителя + «идеальный день» (2026-07-23, собрано;
  ⚠️ ЖДЁТ SQL-блока «ПЛАН ДНЯ» + смоука)** — по решениям владельца:
  • Стрик НЕ менялся (любое 1 занятие продлевает серию); сверху добавлен
    «идеальный день» — все пункты плана выполнены → пламя золотое, строка
    «Идеальный день ✦», клиент пишет activity_log type='perfect' (items_done=0,
    счётчики недель не искажает; тип добавлен в ActivityType).
  • План на Главной теперь ДИНАМИЧЕСКИЙ (lib/dailyPlanCore, чистый, тест
    scripts/test-dailyplan.mjs 8/8): дефолт 3 пункта — Слова (всегда) +
    приоритетный (несданное задание > активный квест > ротация) + ротация
    по дню недели; программа сознательно НЕ в плане (нет измеримого
    «выполнено за день», остаётся строкой в Учёбе).
  • Учитель: раскрывашка «План дня» в карточке ученицы (DailyPlanSection):
    чекбоксы Чтение/Грамматика/Речь/Диалог (Слова 🔒), переключатель
    «задания и квесты сами попадают в план», «Вернуть умный дефолт».
    Хранение: teacher_students.daily_plan jsonb через RPC set_daily_plan
    (только своей ученице, объект ≤4КБ). Клиент: lib/dailyPlan.ts.
  • «Идеальные дни» в диагностике (строка динамики) и отчёте родителям
    («из них N — с полностью выполненным планом»).
  • Смоук scripts/smoke-dailyplan.mjs (6 проверок RLS) — прогнать ПОСЛЕ SQL.

- 🔶 **Заходы 13+14 (2026-07-23, собрано)** —
  • **Свои тексты** (заход 13): кнопка «+ Свой текст» в читалке (EN и ES) —
    вставка / PDF (pdfjs, ленивый чанк 425КБ) / DOCX (mammoth) / TXT; лимит
    15 000 знаков, максимум 10 текстов, хранение ТОЛЬКО в localStorage
    (recall.my_texts.<lang> — чужой копирайт не в БД). lib/myTexts.ts +
    features/reader/MyTextsBlock.tsx; смоук smoke-mytexts.mjs 5/5 (вставка →
    тап → карточка в БД; PDF с текстовым слоем извлекается).
  • **Мультивыбор слов** (заход 14): режим «Отметить слова» в читалках EN,
    «Моих текстах» и заданиях (общий components/MarkableText.tsx; TappableText
    получил markMode/marked/onToggleMark). Тапы помечают слова → плавающая
    панель «В колоду (N)» → lib/batchWords.ts: ОДИН AI-запрос (tier lite,
    чанки по 15) даёт базовые формы + переводы в контексте → addCardsBulk с
    предложениями-примерами. Смоук smoke-markwords.mjs 6/6 (3 слова → 3
    карточки с переводами). ⚠️ ES-читалка пока без мультивыбора (текст там
    по абзацам, нужен общий стейт — отдельным заходом при надобности).
  • Паки: категории «⭐ База уровня / Темы / Идиомы» в PacksSheet (по префиксу
    названия темы; ES без изменений — там только темы).

- 🔶 **Заходы 11+12: «База уровня» из 4000 Essential Words (2026-07-24,
  собрано)** — 6 томов Paul Nation прочитаны vision-конвейером (263 агента:
  Haiku-извлечение по 6 страниц → Sonnet-перевод → сверка 20/20 по исходным
  страницам; финальная сверка перезапускалась через resumeFromRunId после
  session-лимита — кэш вернул 262 готовых агента мгновенно). Итог:
  **3591 слово** (A2 1160 / B1 1215 / B2 1152 / C1 64) в 38 темах
  «База A2 · часть N» (~по 100 слов) — src/data/english/words/words_base.json,
  подключено в words.ts. PacksSheet: категории «⭐ База уровня / Темы /
  Идиомы» (фикс регрессии авто-раскрытия первого уровня под новый ключ
  категория:уровень). Валидатор проверяет словарные JSON (структура,
  связность topic_id). Смоук smoke-basepacks.mjs 5/5. Шаблоны упражнений
  серии — docs/4000-words-exercise-templates.md (новых типов не требуется).
  EN-чанк слов вырос до 753КБ gzip 207 — ленивый, в стартовый бандл не входит.

- 🔶 **Ревью дня 5940d44..HEAD (2026-07-24)** — 5 Opus-ревьюеров по областям +
  2 скептика на находку: 5 находок, ВСЕ подтверждены. Исправлено:
  • ReportSheet: фантомное «— ▼80%» при отсутствии работ в текущем окне
    (guard d.now===null, как в TrendRow диагностики);
  • «Идеальный день» логировался преждевременно по неполному плану (гонка
    async-входов) — входы плана теперь Promise.all с флагом готовности,
    план и perfect считаются только после полной загрузки (+скелетоны);
  • Атомарность: SQL-блок «АТОМАРНОСТЬ» — RPC replace_study_plan (архив+
    вставка одной транзакцией; сбой больше не оставляет ученицу без
    программы) и assign_selected_words (колода+карточки+назначение; нет
    колод-сирот и дублей при ретраях) + клиенты переведены на RPC.
  • Открытая регистрация: в SQL-блок вписано предупреждение — авто-триал
    даёт premium-квоту каждому новому аккаунту (фармится скриптом);
    решение (email-подтверждение / квота триала 20/день / триал по коду
    учителя) — ЗА ВЛАДЕЛЬЦЕМ до открытия.
  ⚠️ Для деплоя нужны SQL-блоки: «ПЛАН ДНЯ» и «АТОМАРНОСТЬ» (старые
  клиентские пути заменены на RPC!). Смоуки после SQL: smoke-dailyplan.

- 🔶 **КЛАССЫ КВОТ AI (2026-07-24, ⚠️ ЖДЁТ SQL-блока «КЛАССЫ КВОТ»)** — крупный
  фикс по наблюдению владельца: раньше consume_ai_quota списывалась на КАЖДЫЙ
  запрос — перевод слова по тапу и попытка произношения стоили столько же, что
  реплика в Диалоге; при лимите 12/день ученица выжигала его десятком тапов, не
  начав заниматься (соседняя дыра: /api/transcribe тоже жёг общую квоту —
  шэдоуинг «съедал» Диалог). Теперь три класса в ai_calls.kind:
  • heavy (Диалог, письмо, квесты, разбор работ, материалы, программа) —
    это и есть «AI-действие» тарифов: 200/сут платный, 12 триал, 5 free;
  • light (перевод слова, определения, батч-слова) — 900/300/100;
  • speech (Groq Whisper в «Речи») — 400/150/50;
  общий часовой предохранитель 200/90/40 от скриптов.
  authorize(req, kind) шлёт p_kind; при старой RPC (404/PGRST202) — повтор
  без параметра, чтобы деплой до миграции не ломал AI. get_my_plan считает
  ai_used_today только по heavy.
- 🔶 **Порядок моделей пересмотрен (2026-07-24)** — RPD у Google ПОМОДЕЛЬНЫЕ,
  поэтому цепочка складывает квоты. gemini-2.5-flash (RPD ~20!) стоял ПЕРВЫМ
  в standard (Диалог — самый частый сценарий) и выгорал за десяток реплик,
  заодно лишая уровень max запасного пути. Новый порядок: standard =
  3.6-flash → 3.5-flash → gemma-4-31b (RPD ~14 400) → 2.5-flash; lite =
  3.5-flash-lite → 3.1-flash-lite → gemma → 2.0-flash-lite; max без изменений
  (Pro впереди). Клиентский параметр model больше НЕ уважается (им можно было
  принудительно стартовать с дефицитной модели и выжечь её всем).
  Генерация под уровень: lite — temperature 0.2 и короткий ответ (перевод
  должен быть точным), standard/max — «размышления» ВКЛЮЧЕНЫ (упираемся в
  число запросов, а не в токены: TPM ~1.9 тыс. из 250 тыс.), max — 8192 токенов.

- ✅ **База синхронизирована с кодом (24.07)** — schema.sql залит целиком,
  смоуки после заливки: квоты+места 9/9, атомарность 5/5, план дня 6/6,
  диагностика 7/7. Работают: классы квот (light/speech не съедают Диалог),
  лимит мест тарифа в join_teacher, advisory-лок против гонки квоты.
  ⚠️ Осталось от владельца: Confirm email в Supabase Auth (договорились —
  после остального) и docs/open-registration.sql (только в день запуска).
  Перед любой следующей заливкой: node scripts/check-schema.mjs.
